# Known Issues & Investigation History (full detail)

Imported from `CLAUDE.md`. See that file for the condensed "Critical / Do Not Touch" summary and the rest of the standing rules.

This is the accumulated, evidence-based record of every significant bug class, architectural fix, and investigation conducted on this codebase. For confirmed **Kylas application** bugs specifically, see `APPLICATION_BUGS.md` — this file covers everything else: code bugs found and fixed, architectural overhauls, and flakes investigated but not conclusively resolved. **Consolidated 2026-09-07** — fully-resolved investigations were compressed to their durable lesson/fix; genuinely open items and standing patterns kept in full. Older narrative detail (blow-by-blow experiment logs) lives only in git history for anything compressed here.

### Session-expiry recovery — architecture (resolved)

`BasePage.withSessionExpiryRecovery<T>()` is the final result of 5 independent fix attempts that each recurred (2026-07-09 through 07-20) before a full-codebase grep found 80 further raw `expect(...).toBeVisible/toHaveText/toHaveURL` calls across Contacts/Companies/Deals/Leads/CallLogs/Tasks/Quotations, all silently unprotected. Verified facts: no cookie-based session — clearing `localStorage` alone immediately redirects to `/signIn`; login is `PUT {apiBaseUrl}/users/login`, JWT in `localStorage.token`, every API call sends `Authorization: Bearer <accessToken>`. A `page.route()`-based interceptor design was built and verified to partially work, then deliberately abandoned: `route.fetch()` itself — even on requests nowhere near a 401 — broke unrelated saves with generic `HTTP 400`s (A/B tested decisively). **`route.fetch()`-based interception is confirmed unusable for this app — do not re-attempt.**

Final architecture: one shared combinator (`withSessionExpiryRecovery`) replaced all 80 raw call sites; `authManager.isSessionExpiryPage()` recognizes both a `/signIn` redirect AND a distinct, URL-unchanged "Forbidden" bootstrap-time page (confirmed no test relies on "Forbidden" as an expected RBAC-denial outcome, so recognizing it as expiry can't mask a real assertion); `AuthManager.ensureFreshSession()` proactively refreshes via headless login when <10 min remain on the JWT's own embedded expiry, wired into `fixtures/index.ts`. `withSessionExpiryRetry(workflowFn)` wraps a whole workflow method and retries it once on detected expiry — deliberately **not** applied to share/reassign/clone/add-from-panel methods (confirmed these assume the page is already on a specific entity's detail view; a blind retry after recovery navigation would fail worse).

`config.buildApiUrl(path)` is the **only** canonical URL-normalization function, born from two separate confirmed bugs: `loginHeadless()`'s hardcoded `/v1`-included assumption broke against CI's differently-shaped `apiBaseUrl` secret (`HTTP 404` on ~64-98 failures per run until fixed), and — found the very next day, in a call site the first fix's own ripple-check had already grepped and dismissed as "logging only" without reading what it did — `DealsPage.fetchCurrentDealApiData()` had an independent copy of the identical mistake, silently swallowed with zero logging on either branch. **Do not hand-roll a third copy; consolidate into `buildApiUrl()`.**

### ID-capture and response-matching (resolved)

`captureXxxIdFromResponse()`-style predicates must require the real versioned `/v1/<module>/` path and exclude `/reports/` — a bare `.includes('/deals')`/`.includes('companies')`-style substring match was confirmed to false-positive against an unrelated background analytics POST (`/v4/reports/deals`) in 3 places (Deals, Companies, a Deals-quotation flow), silently capturing `null` and throwing a false "save failed silently" error despite a genuine success toast. All 3 fixed. **Any new `captureXxxIdFromResponse()` must require the versioned path from the start.**

Session-expiry recovery for `waitForResponse()` ID-capture had two further races, both found via deliberate reproduction and fixed: a flag-based coordination race in `armResponseWaitWithRecovery` where code reacting to the same failure faster than the 10+s recovery (e.g. `assertNoFormErrors()`, ~1.5s) could throw before the flag was ever set — fixed via a synchronous `hasFired()` signal; and a competing-navigation race in `tryRecoverSessionForPage()` where the app's own frontend independently redirects to `/signIn` on the same 401 (`net::ERR_ABORTED`) — fixed with a bounded retry keyed on that exact signature.

### Navigation-drift gap across 6 modules (resolved, 2026-07-27)

Every `waitForXDetailsPage()`/`waitForListReady()` across Deals/Companies/Contacts/Leads/Tasks/Quotations shared the same gap: `waitForUrl()` resolves the first moment a URL matches with no guarantee it stays matching, and the follow-up entity-GET wait was wrapped in a silent `.catch(() => null)` — under real concurrent load the app's client-side router could bounce away before the GET fired, surfacing much later as a generic element-not-found timeout with zero diagnostic signal. Confirmed identical shape in Companies/Contacts/Leads (list+detail) and Tasks/Quotations (detail); Meetings/Call Logs confirmed clean. **Fixed** with two shared `BasePage` methods, `waitForEntityDetailPage()`/`waitForEntityListPage()`, replacing 6 independently-drifting copies — worst case ~190s (down from unbounded 480s), realistic time-to-clear-failure ~35-40s.

Same investigation found and fixed two further bugs: `DealsPage.getAssociatedContactId/Name()`/`getAssociatedCompanyName()` had no explicit timeout at all (this codebase never sets `actionTimeout`, whose real Playwright default is 0/unbounded) — fixed with an explicit 10s bound; and a zombie session-expiry listener left alive by `Promise.race`'s abandoned losing branch in `waitForEntityListPage()`/`TasksPage.waitForListReady()` — could keep listening up to 60s after the calling method returned (confirmed via a real full-suite run: two unrelated recovery attempts fired within 3ms of each other for calls that had returned 27-54s earlier) — fixed by swapping the race's response branch to a plain, listener-free `page.waitForResponse()`.

### Random-option-pick dropdown hangs (resolved)

6 call sites across `DealsPage.ts` (product row, associated contact), `TasksPage.ts`, `CallLogsPage.ts`, `MeetingsPage.ts` picked a random react-select option via an unbounded `textContent()`+`.click()` shape; also found that retrying the *same* index is sometimes futile (one index can be persistently non-actionable while others work fine). **Fixed** via one shared `BasePage.selectRandomOptionWithRetry(options, description, opts?)` — 3 attempts, each bounded to 15000ms, each re-rolling a fresh random index. **Use this for any new "pick a random option from an open react-select menu" call site.**

`DealsPage.fillDealForm()`'s associated contact/company pick is random **by design** (a deliberate 2026-07-05 CI-hang fix, not a bug) — pass `associatedContactName`/`associatedCompanyName` on `DealData` whenever ownership matters for a new Deals test. `BasePage.fillSearchAndWaitForOptions()`'s transient-network retry allowlist (`TRANSIENT_NETWORK_ERROR_PATTERNS`) is deliberately narrow (DNS/connection reset/refused/timeout/unreachable only, explicitly excluding real HTTP 4xx/5xx and routine `ERR_ABORTED`) — **do not widen without the same live-evidence bar.**

### Locator/timing fixes catalog (by module, resolved)

- **Lead multi-select "chip drop"** — `selectRandomFromMultiValueReactSelect()` silently dropped a previously-landed chip; the "reopen menu" click landed on the control div's center, which drifts onto a previous chip's remove icon as chips wrap lines. Fixed by targeting the control's own `<input>` child.
- **Company Phones field collision (Lead)** — a loose substring locator (`input[id*="input_phone_0"]`) also matched Company Phones' first entry. Fixed across Leads/Contacts/Companies by switching to the exact `name="phoneNumbers[0]"` attribute.
- **GPS trigger is section-scoped, not page-wide** — Lead's form has two "Get GPS Address" triggers with identical text; `BasePage.getFormSectionContainer()`/`getGpsAddressTrigger()` scope the click and throw a clear error on ambiguity.
- **`escapeRegExp()`** was duplicated privately across 5 page objects — moved to one shared `protected BasePage.escapeRegExp()`.
- **`QuotationsPage.ts` per-row DOM-read loop** — 3 methods looped every list row with an individual `.innerText()` call; replaced with one batched `allTextContents()` call, 6.4x-38.2x faster across environments. **If you find a `for (...) { locator.nth(i).innerText() }` pattern anywhere else, this is the fix.**
- **`CallLogsPage`'s Associated Deal field** — intermittent "zero options" was a **data-linkage** problem, not search-index lag: `ensureOwnedDealExists()` created the deal with no `associatedContactName`, linking it to a random unrelated contact. Fixed by threading the contact's name through. A second, independent "mis-suspected as index lag" case is documented in the Sandbox Build #144 section below (Products & Services search truncation) — **treat "index lag" as a hypothesis to disprove first, not a default explanation, whenever a live search unexpectedly returns zero/wrong results.**
- **`fillEditForm()` asymmetry (Lead vs. Contact) is intentional, not a bug:** Lead's edit deliberately doesn't re-fill Timezone/Country/Professional fields (scoped to firstName/lastName/Salutation/Requirement/custom-fields only); Contact's does. Currently left as create-only for Lead — extending it is an open decision for a maintainer (riskier: react-select re-selection on a pre-filled form).

### Sandbox CI code-bug batches — resolved, fixed, verified

**CL21/CL23/TC23/TC21/TK24/CL22/CL24 (2026-08-02/03, sandbox run `30706489847`):** 7 code bugs, all fixed and re-verified live. CL21/CL23 — React Select pointer-event interception from a menu left open by a prior interaction; fixed with `openDropdownById()` + `force: true`. TC23 — 3 stacked `waitForTimeout(200)` calls in `BasePage.selectDateTimeCustomField()` pushed a test 6s over its 480s budget; replaced with one condition-based wait for `.rc-time-picker-panel` to hide. CL22 — an unscoped "Log a call" button locator; scoped with `.first()`. CL24 — a too-narrow validation-error locator; widened to include `.alert-danger:visible`. TC21 — a tab click missing session-expiry recovery; wrapped in `withSessionExpiryRecovery()`. TK24 — an unbounded `expect(timeInput).toBeEnabled()`; replaced with a non-blocking 5s check + warning log. Call Log custom-field validation is confirmed **not** a bug — the backend doesn't enforce char-limit/format validation there yet; the corresponding test is `test.skip()`-ed pending backend work.

**5 sandbox failures — CL39/CL32/CL33/D28/Quotations-detail-verify (2026-08-09, sandbox run `31269450132`, confirmed real `--workers=2`/298 tests), all reproduced live before fixing:**
- **CL39** — mis-suspected as a "date-time picker hang," actually a test-data bug: two independent `generateCallLogData()` calls with no `entityType` pinning could produce a `callType` option that didn't exist in the dropdown, and the option-click had no timeout at all (hung the full 480s). Fixed by pinning `entityType` across the test's before/after data, plus adding explicit `waitFor`+`timeout` bounds to `selectFromDropdown()`'s and `fillEntityType()`'s option clicks.
- **CL32/CL33** — confirmed NOT a code bug in `openLogACallForm()`'s retry logic (2/2 clean in single-worker isolation); strongly correlated instead with a genuine Kylas app-side JS `TypeError` under real `--workers=2` concurrent access inside the app's own `openCallLogForm` (see `APPLICATION_BUGS.md` #1). Code-side response: kept the existing correct 5-attempt reload-and-retry structure, added capture-and-log of the real caught error + URL on every attempt so a recurrence is diagnosable instead of a bare "did not open" message.
- **D28** — `DealsPage.cloneDeal()` never verified it was looking at the actual Clone modal (`#editEntityModal` is shared across Edit/Clone/Add-Contact/Add-Quotation); a stale/wrong modal produced a confusing 10s-later "Name field not pre-filled" error. Fixed by adding the same `.modal-title` check sibling flows already used, plus widening the Name pre-fill timeout 10s→20s. Identical hardening applied to `TasksPage.cloneTaskViaEllipsis()` (previously weaker — never checked the Name value at all).
- **Quotations detail-page-after-create** — `QuotationsPage.waitForListReady()` never actually checked readiness (just `domcontentloaded` + a blind 1s wait), combined with `openCreateForm()`'s unbounded button click. Fixed by racing the list container against the create button (mirroring `CallLogsPage`'s proven pattern) and routing the click through the bounded `this.click()` helper. Also fixed the already-documented `fillOwner()` unbounded click in the same file with the bounded-click + 3-attempt-retry pattern.
- A 6th finding from this session's own `--workers=2` stress-testing: CL18 failed twice with two different proximate symptoms (general concurrent-load degradation, no single clean mechanism) — hardened defensively with the same reload-and-retry pattern already proven for `assertRightPanelIconVisible()`, applied to `CallLogsPage.clickEditButton()`.
- **Final verification:** full `deals.rbac.spec.ts` single-worker 25/25 clean; full `call-logs.rbac.spec.ts` under the exact real `--workers=2` condition that produced the original failures — 24/24 clean, no recurrence of any of the above.
- **Call recording `ERR_BLOCKED_BY_ORB`** — previously documented as a confirmed Kylas backend defect, **retracted 2026-08-11**: root-caused instead to this codebase's own `test-recording.mp3` fixture being a 40-byte, essentially-empty ID3-only stub with no real audio frames — a real, validly-encoded MP3 uploaded through the identical flow played back correctly. Fixed by regenerating the fixture with real audio content. Full write-up in `APPLICATION_BUGS.md`'s "Investigated but not confirmed" section.

### Credential leakage into plaintext logs (resolved, 2026-07-20)

`tests/ui/dashboard/login.spec.ts` called `LoginPage.loginWithCredentials()` with real admin/restricted passwords, which flowed into a generic `BasePage.fill()` that unconditionally logged the filled value — writing the real plaintext password into every log file from any run touching `login.spec.ts` (`logs/` is gitignored, never committed, but 5 historical log files on local disk still contain it). **Fixed:** `BasePage.SENSITIVE_FIELD_PATTERN` (`/password|passwd|pwd|secret|token|api[_-]?key/i`) + `isSensitiveFieldDescription()` — `fill()` now logs `[REDACTED]` whenever the caller's description matches (deliberately description-based, not a DOM `type="password"` check). Verified: re-ran `login.spec.ts`, grepped the log — zero real-password occurrences, `[REDACTED]` appears exactly 3 times as expected. Rotating the QA credentials was recommended but not yet acted on.

### RBAC test-isolation and app-bug investigations (resolved)

**Two previously-confirmed "real Kylas application bugs" were retracted 2026-08-11** after re-verification at the user's request found neither reproducible: the Deal Contact-persistence/display bug (a full-reload check showed the Associated Contacts card rendering correctly) and the RBAC company-share-bypass/Meeting-creation race (the existing CR9 repro test passed 3/3 clean, with the caveat that the re-check didn't exactly recreate the original narrow race-timing condition). Full write-ups in `APPLICATION_BUGS.md`'s "Investigated but not confirmed" section.

**Stage's lack of the (now-retracted) Contact-persistence display bug unmasked a separate, genuine, latent test-setup bug in D13b** — it creates its deal with no `associatedContactName`, falling through to the random-pick path; on QA the display bug always made the contact link time out first, masking this ownership-assertion failure entirely. Fixed by creating a contact explicitly and pinning the deal to it. Re-verified 5/5 clean; full 12-file zero-tolerance stage regression: 215/216 (the 1 failure being this exact pre-fix occurrence).

**Lead "Company Lookup"/"Contact Lookup" custom fields (L46/L47, L30/L31) — 9 real code bugs found and fixed across 7 page-object files (2026-07-21/22), each verified 3x on qa/staging/prod:** detail-page carousel reset for a non-cycling Bootstrap carousel (fixed via `revealDetailCarouselSlideFor()`); `CompaniesPage.clickAddCompany()` modal-open race (bounded re-click retry); a transient-backend-400 blind spot in `createCompany`/`createLead`/`createContact` (fixed by classifying the create-POST response by body content, retrying only genuine transient failures); a Close-Lead toggle visibility flake (bounded reload-and-retry, sized off measured real baselines); a ~9-minute share-modal hang across Leads/Companies/Contacts/Deals from an unbounded `userOption.click()` (fixed via `openUserShareTypeSearch()`, bounded click + 3-attempt retry, applied to all 4 modules) and the identical race in `QuotationsPage.selectFromIsInvalidControl()`; `DealsPage.selectFirstOptionFromDropdown()`'s random-pick on Associated Company timing out on a 25+-option unfiltered list — confirmed live on **stage** after an earlier "qa/prod only" scoping had gone stale as stage's data grew (rule 20) — fixed by batch-reading + typing to filter; `ContactsPage.selectFromContactDropdown()`'s identical unbounded-click race (6 call sites, confirmed hanging on **prod**).

**Minor corrections:** CR13's title corrected from "5 permissions" to "4" (Quotation is genuinely inapplicable to Contacts' restricted-user view, not an oversight). Contact+Meeting/Company RBAC — initially suspected app-bug, corrected: genuine, correct RBAC enforcement, test reframed as a proper negative assertion plus a new positive counterpart.

### Test-quality and coverage fixes (resolved)

- **Dynamic-import factory-function pattern** in `leads.spec.ts`'s delete-lead test was uniquely vulnerable to a transient module-resolution race; fixed by switching to a normal static import (grep confirmed the only such usage).
- **Lead's Requirement text field** is a genuine, separate plain text input (`requirementName`) — first field in the section by DOM order, not fourth.
- **Detail-page assertions for Lead/Contact fields** — every field's detail-page container id/rendering format confirmed via direct live DOM inspection, not guessed.

### Investigated but not resolved — inconclusive or unreproduced

Excluded from `APPLICATION_BUGS.md`'s confirmed bar. **Do not re-close any of these without new evidence (rule 21) — a single clean local reproduction does not disprove a load-dependent flake.**

- **Unexplained Deals flake (2026-07-06):** logging a Call on a deal shared with the restricted user intermittently fails with a permission error even when sharing is verified correct. Six controlled experiments, every theory disproven by the next, no consistent mechanism found; downgraded to "uncertain" (two later-discovered client-side bugs could have contaminated the original experiments). Do not spend further Playwright-side time on this without backend/network access.
- **`quotations.rbac.spec.ts:380` 8-minute timeout (2026-07-20):** one CI occurrence; `openCreateForm()`'s click has no per-call timeout and this codebase sets no global `actionTimeout`. 5/5 clean local reproductions. This raw-click-with-no-bound shape is the prevailing style throughout `QuotationsPage.ts` — not a safe one-line fix on this evidence alone; accepted as an unresolved one-time flake.
- **`call-logs.spec.ts:391` company live-search "no options" (2026-07-20):** one occurrence, unconfirmed correlation with rising swap usage (390Mi→511Mi→683Mi). Needs recurrence to settle.
- **`meetings.spec.ts:120` reschedule failure (2026-07-20):** one occurrence, correlated with three `HTTP 500` responses on meeting-invitee lookup in the same test; passed cleanly on retry. Suggestive, not confirmed.
- **`tasks.rbac.spec.ts:69` edit-modal hang (2026-08-03 area):** 0/5 local reproductions. Defensively hardened per rule 10 — `selectReactSelectOption()` now waits for the menu to actually close after an option click.
- **HTTP 500 on meeting creation and Deals RBAC Task-permission 8.2-min timeout (2026-08-03):** both formally **INCONCLUSIVE** — pass-in-isolation/fail-in-suite pattern suggests environmental degradation, but a code-side cumulative effect (RBAC permission-check caching, N+1 under accumulated data) couldn't be ruled out for lack of instrumentation. Short version in `APPLICATION_BUGS.md`'s bottom section.
- **Profiling hypothesis disproven, do not re-attempt (2026-07-17):** 2.4h of a 6.5h run sat in a post-login gap; `waitUntil: 'domcontentloaded'`-strictness was tested live and disproven (3.8-5.2s regardless of value, nowhere near the observed 22-25s gap). Real cause is almost certainly genuine QA-environment load accumulation, not a client-side bug.
- **DB3 ("expand a collapsed section") — REMOVED 2026-09-02, not fixed.** A concurrency hypothesis for the intermittent `aria-expanded` stuck-at-`false` timeout was directly tested and disproven (a fully isolated, zero-concurrent-activity re-run still failed on its first attempt). Removed rather than debugged further per explicit direction — coverage judged not essential enough to justify continued investigation. `expandSection()` itself is not dead code — still exercised by DB2/DB4's own setup/cleanup.
- **Dashboard render-race under Grouped Smartlists/Smartlist for Email and Contact — OPEN.** A real test-side defect (the old 2-attempt `addDashlet()` design cancelling its own first attempt's in-flight confirmation request on retry) was found and fixed, but re-running the full 17-test block post-fix reproduced failures on the **same two tests, DB33 and DB36**, via two different proximate patterns: DB36's confirmation-fetch is never issued at all within the 60s window; DB33's fetch succeeds (HTTP 200) yet the dashlet still isn't found — a live manual walkthrough ruled out a text-matching bug entirely, unable to reproduce the scenario manually at all. Leading (unproven) hypothesis: the dashlet's component fetches data successfully but intermittently fails to paint a visible DOM element — a genuine client-side rendering defect only under real rapid-fire multi-dashlet-add load (18/18 isolated single-test runs passed cleanly both before and after the fix). Possibly the same mechanism as the stuck-dashlet symptom documented in the Dashboard section below — not confirmed. No further fix applied; app-side rendering question remains open for a future session.
- **`reports.spec.ts` UI test count drifted 37→38** between 2026-08-22 and a 2026-09-02 recount — not investigated. Re-verify via a fresh `--list` run before relying on either figure.

### Products & Services — confirmed mechanisms and quirks

Live-confirmed via DOM inspection/network capture/direct reproduction, not inferred — see `PRODUCTS_AND_SERVICES_PROGRESS.md` for full build/investigation history.

- **CKEditor 5's description field must be reached via its internal data model, not the DOM** — a plain `.fill()`/`.type()` only mutates the rendered DOM, not the model serialized into the save payload. `setDescriptionViaCkEditor()` calls `.ckeditorInstance.setData(text)` directly on the live editor instance.
- **Units is a genuine multi-select, but every workflow only ever picks one option.** Opening the menu needs `ArrowDown` after focus (async "press Down to open"), not an immediate click. **CRITICAL incident:** `selectFromReactSelect()` originally had no clear-existing-chip-first step — harmless for single-select (a new pick replaces the old) but destructive for Units (a new pick only ever adds, and an already-selected option is excluded from the menu, so a second call targeting it finds nothing and times out). This corrupted the permanent `adminActive` fixture (id 53242) before being caught, root-caused, and fixed (bounded chip-clear-first logic); the live fixture was restored via an approved read-modify-write PUT and re-verified via GET diff.
- **Product name uniqueness is enforced live, inline, as-you-type/on-blur** (`GET /products/has-duplicates?...`), not at save time — `assertDuplicateNameFieldError()` asserts against this live field state.
- **No edit icon anywhere in the UI** — row-click navigates directly to `/setup/products-services/edit/{id}`, which doubles as both view and edit surface (no detail page exists at all).
- **Deal edit + product add triggers a real "Distribute Equally" reconciliation requirement — a real feature, not an app bug.** A body-level portal modal (`.installments-modal.distribute-modal`, NOT nested inside `#editEntityModal`) must be resolved before Save's click handler does anything (confirmed via exhaustive network capture — zero requests attempted until resolved). `DealsPage.handleDistributeUnallocatedAmountIfPresent()` drives this automatically. **Standing lesson:** this was initially, incorrectly concluded to be "an unfixable app bug" from headless investigation alone — the user verified live in headed mode it was a working feature the automation was simply missing a step for. **Never conclude "app bug" from headless/automated evidence alone — verify live, headed, first.**
- **An intermittent "element detached from the DOM" race on custom-field date pickers during Deal edit — root-caused partially, fixed, exact trigger unconfirmed.** `BasePage.selectDateCustomField()`'s final day-cell click failed in ~50-58% of real Deal create+edit cycles, always on edit's first calendar interaction. The suspected "previous calendar didn't finish closing" mechanism was directly disproven via live `MutationObserver` instrumentation (all of a Deal's date-picker widgets are permanently mounted, only CSS-hidden). A 3-attempt reopen-and-retry fix was verified via 12 real runs to have a **0% retry success rate** (all real recovery came from the pre-existing typing fallback) — simplified to 1 attempt accordingly; re-verified 24/24 across two batches. Leading (unconfirmed) theory: `fillEditForm()`'s rapid-fire field fills (~60-120ms apart) can leave a React re-render lagging right as the first calendar click fires. This is a shared `BasePage` method used by all 9 modules with date custom fields — the warning is expected, self-healing behavior, not a regression.

### Consolidated open items — Products & Services session (2026-08-10)

Item numbers are stable references (cited elsewhere in this file and in code comments) — **do not renumber.** Resolved items compressed to one line; only genuinely open items keep full detail. Full evidence: `PRODUCTS_AND_SERVICES_PROGRESS.md`.

**CRITICAL incidents (both resolved, kept as pattern references):** 1. `adminActive` fixture corruption via Units multi-select — fixed (bounded chip-clear logic), fixture restored, re-verified. 2. DOM-detachment date-picker race — fixed (1-attempt-then-typing-fallback), 24/24 verified; root trigger still unconfirmed.

**Genuinely open:**
- **6. Real JWT token lifetime is NOT a stable constant across investigations** — three different measured/assumed values (~10.4h documented, ~20min found once, ~101min on a later re-check, reproduced 4/4). `AuthManager.ensureFreshSession()`'s 10-minute proactive-refresh threshold was sized against the wrong (10.4h) assumption — even the ~101min reading gives only ~10x margin, not the assumed ~62x. Deliberately deferred — needs a dedicated investigation into WHY the value varies before any new margin can be trusted; touches shared `AuthManager` code used by every test.
- **12. RE-OPENED 2026-08-11, still open.** A ~40-minute-old `storageState` landing on sign-in was previously "explained" by item 6's then-believed ~20min JWT lifetime — no longer holds now that item 6 found ~101min. Genuinely unexplained again; grouped with item 6 for a future session.
- **17. CONFIRMED live 2026-08-17, NEEDS FIX.** `assertQuotationInList()`'s search-based existence check can false-positive on this codebase's long-lived, never-cleaned QA dataset — `/v1/quotations/search` does fuzzy OR-word matching, not exact-phrase, and results cap at 10 with no check the actual target is among them. Affects every test using this assertion pattern. Not yet fixed.
- **19. Open cleanup, not attempted.** Native (non-custom-field) date pickers on Deals/Quotations/Call Logs remain 3 independently-maintained private `selectDateInPicker()` copies, unlike the genuinely-shared custom-field version.
- **20. Open optimization, not attempted.** A `.custom-month-year-dropdown` direct jump control exists on every date-field calendar, benchmarked ~41% faster for non-adjacent-month jumps, but is unadopted anywhere.
- **21. Standing gotcha, not fixed.** `npm run test:<module> -- <args>` silently appends passthrough args to the LAST command in the `&&`-chained script (`playwright test ... && npm run notify`), not the first. Use `npx playwright test <paths> <flags>` directly instead.

**Pre-existing prettier drift (out of scope, purely cosmetic):** `QuotationsPage.ts` (~20 locations), `LeadsPage.ts` (7), `BasePage.ts` (5).

**Already resolved (listed only so a `[DEFERRED]`-tag read doesn't re-open a closed item):** `assertForbiddenOnRestrictedEdit()`'s 403/`00902001` handling; PS10/PS11 Distribute Equally fix; Batch 7's 12/12 regression; a one-time subagent no-git-rule disclosure with no recurrence.

### Reports module — confirmed findings

Two confirmed real application bugs from this work (`removeDimension()` client-side `TypeError`, a non-functional "Report Name" sort control) are in `APPLICATION_BUGS.md` #2/#3, not here.

- **A third-party-widget-triggered react-select race is repo-wide, not Reports-specific — fixed, pattern documented for reuse.** The `viasocket.com` chatbot widget on every page can fire an async DOM mutation 20-160ms after a react-select menu opens, closing it before an automated click lands. Two approaches were tried and ruled out: blocking the widget's domain via `page.route().abort()` hung the whole browser session 30 minutes (retried aggressively); waiting for the widget's own network response first was insufficient (the disruptive re-render lands 130-160ms *after* the response). **Proven fix** — open, wait visible, then confirm the menu **survives a stability window** before trusting it (a `waitFor({state:'hidden'})` timeout is the success case) — documented in `.claude/reference-patterns.md` §18, verified via 45 combined trials with zero failures. Check for this race before assuming a module-specific bug on any "menu closed before I could click" symptom.
- **`getDestinationListTotalCount()`-style reads should default to `span.update`, not `#totalRecords`** — confirmed universal across Lead/Deal/Contact/Company/Task/Meeting/Quotation/Call Log; `#totalRecords` isn't reliably present.
- **Call Log's Date Filter dropdown has no "Created At" option** — its real equivalent is "Logged At" (confirmed option list: Logged At, Start Time, Date, Updated At, Date Time Picker). Don't assume a date-filter option list is identical across entity types without confirming live.
- **Playwright's `dependencies` project feature is confirmed unsafe for this repo's CI shape — do not reach for it for any "run after everything else" need.** A `--project=X` depending on another via `dependencies: [...]` silently bypasses `--grep` filtering for the dependency, even when co-selected explicitly on the same command line (confirmed via a disposable scratch project). Invoking the two projects in separate commands instead re-runs the whole dependency a second time (no cross-invocation dedup, confirmed via `--list`).
- **Run-count/aggregate verification design: self-contained per-test windows, not whole-suite-spanning.** `globalTeardown` was investigated for a whole-suite design and rejected for two concrete reasons: (1) a `globalTeardown` failure is invisible to this repo's own notification email — `ReportParser.ts`/`NotificationService.ts`/`EmailTemplate.ts` never read that field; (2) `globalTeardown` gets no `Page`/fixtures/session-expiry recovery/`ErrorCollector`, so matching current UI-level drill-through depth would mean hand-launching a browser with none of the framework's safety nets. A dangling, unmerged branch (`feature/reports-module-20260817`) attempted a more ambitious global creation-ledger approach — a genuinely better starting shape if whole-suite verification is ever revisited — but had its own confirmed bugs (the same `dependencies` flaw, and an incomplete ledger covering only 2 of many Lead-creating call sites).

### Sandbox Build #144 (2026-08-22/23) — resolved, fixed

Sandbox run `32575703303` (`ENV=staging`, real `--workers=2`, 379 tests). Full standalone evidence: `.claude/sandbox-build-144-*.md` files (kept as investigation notes, not merged wholesale here).

**Deals clone — a real data-correctness gap, not just a flaky assertion.** `cloneDeal()`'s pre-save readiness wait recurred as a genuine timeout under real load — confirmed NOT caused by any code change, the cross-worker credential race, or CPU throttling (up to 40x, not reproducible). A first fix attempt (make the wait non-fatal, proceed anyway) was verified with real `--workers=2` trials to catch a genuinely worse problem: a clone saved with the stale pre-"Copy" name. **Real fix:** retry the whole modal-open-and-settle sequence (bounded 2 attempts) before any proceed-anyway fallback; verification redesigned to check a stable ID-based signal (capture the new ID via a network response, assert it differs from the original, verify the name on the clone's own separately-loaded detail page) instead of the modal's mid-render state — see `.claude/reference-patterns.md` §4's "ID-based clone verification." Verified 30/30 clean trials.

**Products chip-clearing — an async-hydration race, not chip-removal failing.** `clearAllProducts()` threw "chips still present after the clear loop reported done" — the inner loop legitimately read zero before chips reappeared, because the Requirement section's async product data hadn't finished hydrating before clearing ran. **Fix:** a bounded 4-round outer "settle" loop confirming the zero state actually holds (stability-window idiom, §18) before declaring done. Verified 20/20 post-fix, 0 regressions across 45 total trials. A genuinely pre-existing `waitForTimeout(150)` was also found and removed from the same method during this work — caught by the real pre-commit hook when re-indentation moved it into the staged diff, a real example of the hook catching old debt, not just new code.

**Product-search truncation — a second confirmed "index lag was the wrong first guess" case.** `BasePage.searchAndSelectByName()` computed its search term as the FIRST WORD only, and every Products & Services fixture starts with the shared `[QA-Auto]` prefix — searching that alone returned a hard-capped 50 alphabetical results with the target absent. **Fixed:** search with the full name instead (matching the sibling `addProductRowAndSearchByName()` pattern), zero-risk (one caller in the codebase). Verified 21/21 real trials.

**Deferred, not fixed this pass:** 5 background-error URL patterns not yet whitelisted in `errorFilters.ts` (all the same already-accepted "background widget aborts on navigation" class, none correlating with any real failure) — good candidate for a future dedicated cleanup. GitHub Actions' web log viewer can appear to freeze on a long high-volume run — confirmed a UI limitation, not a pipeline defect (the raw log via `gh run view --log` kept writing normally throughout) — always pull the raw log before concluding a CI run has hung. A classification regex built from one captured failure signature can miss other signatures of the same underlying error (confirmed for the Reports `removeDimension()` crash filter, which matches on `fieldType` text but missed 3 later occurrences that only captured a generic `"ErrorEvent"` string) — verify a new noise-filter regex against every captured variant, not just the sample it was written from.

### Notification/reporting pipeline — findings (2026-08-24)

- **Playwright's raw JSON report's `results[].errors[]` (plural, every error in order) can hold a strictly more useful message than `results[].error` (singular, always just `errors[0]`).** `ReportParser.ts` only read the singular field before this — confirmed live on a historical flaky test where `error.message` was a generic timeout but `errors[1].message` was the real, actionable cause. Any future raw-report reader should read the full array (last entry is most proximate to the actual give-up), not just the singular field.
- **A task briefing can describe a feature that was never actually built in the code under investigation.** A redesign brief referenced a "Reports Verified" email section that doesn't exist anywhere in `EmailTemplate.ts` (zero grep matches) — likely conflated with the unrelated Reports UI module, or a memory of an abandoned branch. Verify directly (grep, read the file) before building on a described feature's assumed existence.
- **Enrichment-layer convention, worth reusing:** `FailureDetailBuilder.ts` keeps raw fs/parsing access isolated in a dedicated small loader module, keeps the derivation logic itself pure (no fs/network calls, testable via a plain context object), and lets the caller wire the loader's output into the pure function's input — mirrors `RunHistory.ts`/`AutomationHealth.ts`/`FailureAnalyzer.ts`'s existing convention.

### CI reporting-history ledger — root cause confirmed, fixed and merged to dev (2026-08-24/25)

`ci/reporting-history` (feeding Module Analytics' trend column and recurring-flaky/regression detection) had never received data in any real CI run, ever — masked by its own "never fail the build" try/catch. **Real root cause (found only on a third investigation round, after two rounds chased a false signal):** `syncHistory.ts` clones into its own fresh temp directory to do git work, and `actions/checkout@v5`'s `token: secrets.PIPELINE_TOKEN` only authenticates the *original* checked-out workspace — never passed through to the temp clone. Every push was fully anonymous, doomed to fail 100% of the time. A compounding bug hid this for over a month: the push-failure handler unconditionally logged "rejected (concurrent update)" without ever reading the real captured git stderr. Two earlier fix rounds (fixed-delay retry, then elapsed-time-bounded backoff) were both real, correctly-engineered fixes for a race that was never actually the cause — `git ls-remote` returned empty throughout both, proving nothing had ever won the "race" they were tuning for. **Standing lesson: a log message is not evidence** — this bug survived two careful fix rounds because both trusted an unverified log line instead of the real git error text it claimed to summarize.

**Real fix (merged via `4477e0f`):** `gitAuthEnv()` injects the identical Basic-auth header `actions/checkout` itself uses via `GIT_CONFIG_*` env vars (chosen over URL-embedding, which would print the token on any failing command); all logging routes through `log()`/`warn()`, which unconditionally scrub the registered token from any string; `classifyGitFailure()` replaces the old unconditional guess with an ordered list of named classifiers checked against the real error text, falling through to an honest `unclassified` (never retried) when nothing matches. `dev.yml`/`staging-promotion-gate.yml` were also missing from the original permissions fix — all 7 workflows invoking `history:sync` now have `permissions: contents: write` + a credentialed checkout token. Verified: auth-header transmission byte-for-byte against a local HTTP server; classifier + redaction logic 12/12 checks; the original concurrent-race reproduction re-run 3/3 clean. **Not verifiable locally:** a real push succeeding against github.com with the real token — this repo's git rules prohibit any push from a session; confirmed only by a real CI run.

**CONFIRMED WORKING as of 2026-09-07 — the "had never received data in any real CI run" framing above is now stale.** During PROD Build #4 investigation work, `git fetch`ing the `ci/reporting-history` branch (read-only) found 8 real historical run records in `history/prod.jsonl` (including genuine `runSource: "jenkins"` entries, dated from 2026-08-27 through 2026-09-07) — the fix above is confirmed live-working, not just merged. This paragraph is left in place as the incident history; only the "still never received data" characterization is corrected.

### Sandbox Build #147 (2026-08-24/25) — resolved

Four flaky/failing tests, each investigated independently, live-verified on staging.

1. **`deals.rbac.spec.ts:719` (243s duration)** — confirmed working as designed: genuinely hitting the Navigation-drift-gap fix's documented ~190s worst-case ceiling on a test chaining several such navigations. No bug, no change.
2. **`reports.spec.ts:902`/`reports.rbac.spec.ts:709` (R36/R64) — real bug, two layers, both fixed.** Layer A: `LeadsPage.deleteLead()` had no network confirmation after the delete-confirm click, unlike `DealsPage.deleteDeal()` — fixed by mirroring that pattern (also fixed R64). Layer B, found after Layer A alone proved insufficient under real load: `verifyRunCountForEntity()`'s report uses a coarse ±1-calendar-day window (a hard app UI limitation) not scoped to the one entity under test — under concurrent execution other tests' Leads in the same window could mask the signal. Fixed by giving the method an optional `filters?` param so R36/R64 tag their Lead with a uniquely-timestamped `lastName` and filter exactly. Also fixed: a correctly-zero-match report renders no header at all — `getReportTotalFromHeader()` now races the metric-name locator against the no-data message. **Dual date-window capability added 2026-08-25** (a ±5-minute-padded `narrowWindow` alternative for tests with no uniquely-taggable field) — byte-identical default behavior confirmed by grep across all 18/5 existing call sites; live-verified via new test R65, 3/3 clean.
3. **`reports.spec.ts:302` (Meeting report-count) — two unrelated symptoms.** (a) A `safeWaitForURL` timeout inside `createReport()`, root-caused via live reproduction to a genuine backend `HTTP 500`/`code: "01403004"` on **100% of first Save attempts specifically for Meeting reports** (immediate retry always succeeds; confirmed Meeting-specific across 20 cross-entity attempts) — see `APPLICATION_BUGS.md` #4. Mitigated with a bounded (max 2 attempts) retry on that exact signature, benefiting all 14+ callers. (b) A `Test timeout` hang inside fixture teardown — ruled out as the known fixture-timeout classes, 0/9 reproduced; no fix applied (no reproduction, high blast-radius code).
4. **`companies.rbac.spec.ts:126`/`:190` (COR7/COR9) — unrelated causes.** COR7 — real bug: `CompaniesPage.saveEditedCompany()` had no network confirmation before the caller searched under the new name, the same class already fixed for Leads — fixed by mirroring that pattern; verified 22/22 under real `--workers=2`. **Flagged, not fixed:** `ContactsPage.saveEditedContact()` has the identical gap and IS actively exercised by 2 real call sites — apply the same ~15-line fix in a future session. COR9 — not a Companies bug; matches the already-documented `browser.newContext()`/`isSessionValid()` infrastructure class, not reproduced at file-scale.

### `reports.spec.ts:760` flaky `browser.newContext()` Protocol error (2026-08-25/26) — resolved

**One occurrence** (sandbox run `32839778416`): a thrown CDP Protocol error during fixture setup, late in a long multi-worker run — resource-pressure-consistent but root cause of the Protocol error itself **not confirmed** (not reproducible on demand, no resource metrics available), left open per rule 10, not hardened.

**A separate, real, well-evidenced structural gap was found and fixed while tracing the stack:** `createRolePage()`'s recovery-login call had no try/catch, so any transient failure during it (this Protocol error or anything else) ended the whole fixture setup on just 1 of its intended 2 attempts. Fixed by wrapping it in try/catch and falling through to the next iteration.

**REVISED 2026-08-26** — a follow-up sandbox run (`32857739191`, 379 tests) recurred with a related-but-different symptom, plus surfaced 2 unrelated bugs in the brand-new Reports narrow-window feature; all fixed:
- **Tasks RBAC** — the retry-loop fix above worked correctly, but the loop's next step (`getContextForRole()`) was then killed by CI's own governing 120s test timeout, because `navigateAndConfirmLoggedIn()`'s own legitimate worst case (up to ~240s of goto retries + URL race) can exceed that ceiling on its own before recovery logic gets a fair chance. A first fix attempt (statically padding by a guessed +120000ms) was correctly rejected as disguised environment-specific patch work. **Real fix, deadline-aware:** every fixture-setup stage now consults one real deadline (`Date.now() + testInfo.timeout * 0.85`, scaling automatically with whatever timeout is in effect) — waits are capped at `min(nominal, timeRemaining)`, and the outer loop fails fast with a clear diagnostic reason once remaining budget can't support another attempt, instead of a doomed attempt being silently killed by Playwright's own opaque timeout. Verified via 12/12 pure logic checks on the deadline arithmetic/branch decisions.
- **`verifyRunCountForEntity()`/`waitForReportTotalBelow()` were reading `reportTotal` from a DOM scrape (a `data-original-title` tooltip), not the real API response that computes it — fixed per explicit direction to stop treating the DOM as authoritative.** Live network capture confirmed the real source: `POST /v3/reports/<plural>?...` returns an array of dimension-value buckets whose `values[0]` sum exactly equals the header's displayed total. Fixed with `armReportDataResponseCapture()`/`sumReportDataResponse()`, replacing the DOM scrape for these two call paths specifically (kept unchanged for `getReportTotalFromHeader()`'s many other unrelated callers). This does NOT replace the separate `/v1/search/*` ground-truth check, which exists specifically to catch a genuine report-engine bug. Verified live across every entity type's real API pluralization (including irregular `calls` for Call Log).
- **`getApiCountForWindow()` never applied the caller's `filters`** — only `createdAt between [from, to]` — so R36's tight window could legitimately also contain a concurrently-running R65's Lead, producing a false "report undercounts." Fixed with an optional `filters?` param mapped through a deliberately narrow field map, live-confirmed against real staging. Verified: R36+R65 concurrent 2/2 clean, R64 1/1, R4 (unaffected caller) 1/1.
- **`fillTimeInPicker()` computed hour/minute/day from the running process's own LOCAL timezone**, not the fixed Asia/Kolkata timezone the app always interprets the Custom Date Range in — invisible on this session's own IST dev machine, but wrong on GitHub Actions' UTC runners (a real ~13-hour window miscalculation). Fixed with a `toIstWallClockDate()` conversion helper, scoped only to the `narrowWindow` path. Verified live under `TZ=UTC` (recreating CI's exact timezone) and under real `--workers=2` concurrency.

**Miscellaneous, already resolved:** Annual Revenue field in `CompaniesPage` — removed 2026-08-11 as a stale non-issue (live-verified it was never actually broken). QA sessions expire after ~1 hour (`rm -rf src/auth/storageStates/qa/` forces re-login). Clone lead/contact creates a pre-filled form — change email/phone to avoid duplicate-validation errors. `saveQuickTask()` waits for the standalone task list view — use `saveQuickTaskFromEntityDetail()` when called from an entity detail panel.

### Dashboard module — confirmed findings (2026-09-01/02)

Built following the standard module pattern with no structural deviation (unlike Products & Services). Full narrative lived in a working file, deleted once this consolidation was confirmed complete.

- Full Add-Dashlet wizard mechanics (3 dashlet types, entity availability, Report-type's Reports-module dependency, Grouped Smartlists' multi-select): `.claude/reference-patterns.md` §20.
- Collapse/expand state is account-persisted, not session-scoped (survives a full reload); edit mode disables collapse/expand entirely.
- A fresh page load always lands on the account's PRIMARY dashboard, never the last-viewed one — any "persists after reload" check on a non-primary dashboard must re-select it from the switcher after reload.
- The dashboard-level edit-mode Cancel always opens a "Discard Dashboard" confirmation modal, even with zero pending changes.
- Dashboard Name has no stable selector of its own — `.dashboard-name input` is the only option (rule 17 carve-out).
- A dynamically-created, entity-scoped section (`id="{entity}_section"`) appears the moment that entity's FIRST dashlet is added; two different entities' dashlets never merge into one section.
- **A stuck-dashlet SYMPTOM was observed and reproduced on Default/Productivity Dashboard, root cause never established** (several pre-existing Report-type dashlets stayed on their loading placeholder with no remove action present in the DOM). Reported strictly as a reproduced symptom, not filed as a confirmed app bug — deliberately never exercised by any test in this module (every dashlet-adding test targets a disposable custom dashboard).
- **The original single-test-loops-17-combinations design (DB8/DB20) was replaced with 34 individual tests, each creating and deleting its own disposable dashboard.** An intermediate shared-dashboard-per-role design (via `beforeAll`/`afterAll`) was tried and abandoned the same day — confirmed live that `fullyParallel: true` does not guarantee one `beforeAll`/`afterAll` pair per describe block (a real run showed 3 separate dashboards existing across one 17-test run). `.serial` mode was considered and rejected (skip-cascades every remaining test after one failure). Verified via 2 clean full-block runs per role plus a real full-suite pass.
- **`openMarkAsPrimary()` fired its click and returned immediately with no wait for the underlying POST — fixed.** Confirmed real via a live DB14 failure (reload raced ahead of the still-in-flight mutation) and a DB26 occurrence (a genuine `HTTP 403` the old fire-and-forget click had no way to surface). Fixed by arming `armResponseWaitWithRecovery()` for the real endpoint before clicking and throwing on a non-ok response. Verified 3/3 clean on both.
- Restricted role cannot self-mark "Default Dashboard" as primary (`mark_as_preferred` → `HTTP 403`, `errorCode: 024002`) — a real, by-design RBAC boundary, not a bug. Fixed: dashboard teardown no longer force-restores primary; deleting a disposable primary dashboard auto-falls-back to Default Dashboard for both roles.
- **`switchToDashboard()` had a real, previously-silent teardown-failure gap — fixed 2026-09-03, underlying trigger still not understood.** A single immediate, unretried read of `getCurrentDashboardName()` after the switcher click could catch the previous dashboard's still-showing title before the target's async re-render landed. Fixed with a bounded 3-attempt retry + `expect.poll()`. Verified 6/6 clean — but notably, attempt 1/3 failed on all 6/6 runs, always during teardown specifically, never during the test body's own earlier switches; the retry reliably masks it, but WHY teardown's first switch specifically stalls has not been root-caused.
- **`deleteDashboardByName()` had a real CI-specific test-timeout gap — fixed 2026-09-03, confirmed NOT a concurrency bug.** Both DB10/DB22's own `finally` blocks call `deleteDashboardByName()` a second, "guaranteed no-op" time on an already-deleted dashboard — which fell into `switchToDashboard()`'s click finding nothing and hanging for the full `NAVIGATION_TIMEOUT` (which in CI exactly equals the outer test timeout, leaving zero headroom). Fixed by presence-checking via `isDashboardInSwitcher()` before ever attempting a switch. Verified 12/12 clean, including under `--workers=2` with CI's real timeout values.
- **One-off DB32 failure (same sandbox run)** — a static-asset `HTTP 503`/`ERR_ABORTED` on the app's own core JS bundle prevented the dashboard page from ever booting; confirmed a generic infra blip (self-healed on retry), not the render-race above and not a code bug.

### `globalSetup.ts` product-reference-data `HTTP 400` on staging — OPEN, unresolved (2026-09-04)

`globalSetup.ts`'s `ensureProductFixtures()` → `loadProductReferenceData()` threw `HTTP 400` on `GET /products/layout?view=create` before a single test could start — this call is an unconditional cross-module prerequisite, so it blocks **any** staging run, not something Reports/Quotation-specific. Reproduced 2/2 (`ENV=staging`, `--workers=1 --retries=0`). QA unaffected — `src/data/productFixtures/qa.json` written successfully multiple times the same day.

**Complicating, unreconciled data point:** `staging.json`'s fixture cache contained real, valid data timestamped ~18 minutes before a repro attempt — meaning fixture creation genuinely succeeded very recently, inconsistent with a permanently-broken endpoint. Not reconciled.

**Follow-up investigation (same day) ruled out several theories with direct evidence, none conclusively:** a header diff (browser vs. Playwright's `APIRequestContext`, 10 headers missing) looked like a WAF/bot-detection lead but was weakened by a plain Node `fetch()` with an even more bot-like signature succeeding 17/17 times; token format confirmed matching the real browser token byte-for-byte; code/config/credential regression ruled out via `git log`/`gh secret list` (nothing touched in weeks/months). **A real, dated CI-topology change was found and directly corroborated:** commit `c85a7b3` (2026-07-28) switched `sandbox.yml` from QA to staging, so staging now absorbs two frequent/heavy `--workers=2` pipelines — a real concurrent `stage.yml` run was confirmed `in_progress` through the exact window of a repro failure. An endpoint-specificity probe (200 real requests across 4 endpoints during a confirmed concurrent `stage.yml` run) found **zero failures on any endpoint**, including the target one — inconclusive; timing data actually argued against "layout is uniquely heavy." One variable never tested: every probe used a single lightweight login, not `globalSetup.ts`'s real sequence of two full browser logins immediately followed by the raw API call — replicating that exact sequence would mean running `globalSetup.ts` itself.

**Overall assessment:** most likely real, intermittent backend contention correlated with staging's heavier CI load since 2026-07-28 — not fixed, not root-caused to certainty, flagged for a future session with real backend/infra access.

**Mitigation applied then REVERTED (2026-09-04→05).** `stage.yml`'s workers were dropped 2→1 as an explicitly-labeled, never-confirmed mitigation. This roughly doubled the workflow's real runtime (~4.6h avg at workers=2 → a confirmed ~9.9h at workers=1, corroborated by two independent measurement methods converging on the same ~550-600min figure), pushing it past GitHub Actions' 360-minute default job timeout — the job was force-killed mid-suite at 275/455 tests, cascading into a skipped history-sync and a crashed notify step. Since the mitigation was never confirmed to fix anything and went on to cause a real, confirmed failure, it was reverted outright rather than extending the timeout to accommodate an unproven change. **The original staging-load-contention question remains genuinely open.** `stage.yml` deliberately was NOT given an explicit `timeout-minutes` post-revert — 5 real historical `--workers=2` runs (253-299 min) never approached 360min, but this margin (~17%) is flagged as "watch, don't fix," not risk-free, given the suite keeps growing.

### GitHub Actions default 360-minute job timeout — CORRECTED 2026-09-09, the 2026-09-05 fix below was NOT actually a fix for jobs needing >6h

**This section's original "resolved (2026-09-05)" framing was wrong and is corrected here with direct evidence, not retracted quietly.** Found while investigating the above: none of the 6 workflow files had an explicit `timeout-minutes`, relying on the 360-minute default. Real measured durations: `dev.yml` 13.4min, `prod.yml` 6.5min, `sandbox.yml` 87.5min (all huge margin) — but `qa.yml` (287-324min across 5 real runs) and `main.yml` (274-309min across 2) were both sitting as little as ~36-51 minutes below the same cliff that just killed `stage.yml`, for unrelated reasons (legitimate `@regression`/full-suite runtime plus natural suite growth). The fix applied then — `timeout-minutes: 480` (8h) added to `qa.yml`/`main.yml` — was based on an incorrect premise: that raising `timeout-minutes` past 360 buys real extra headroom on GitHub-hosted runners.

**Proven wrong 2026-09-08/09, with direct evidence, not just theory.** On 2026-09-08, sandbox (run `34217579300`), qa (`34256019455`), and stage (`34256174792`) were all cancelled — investigated with the same rigor as everything else in this file (`gh run view`/`gh run view --log`, not speculation). All three carry GitHub's own literal annotation: **"The job has exceeded the maximum execution time of 6h0m0s"**, at 6h01m17s / 6h01m35s / 6h02m10s respectively — all still actively mid-execution (`npx playwright test --workers=2`, live browser processes) when killed, not hung or crashed. Critically, **`qa.yml` had `timeout-minutes: 480` explicitly set at the time and was killed at 6h01m anyway** — identical to `sandbox.yml`/`stage.yml`, which have no `timeout-minutes` at all (defaulting to 360). This proves GitHub Actions enforces a **hard, non-configurable 6-hour maximum execution time per job on GitHub-hosted runners** — setting `timeout-minutes` above 360 does nothing; the platform ceiling cannot be raised, only approached-and-hit sooner or later depending on real suite runtime. Ruled out as alternative explanations, each with direct evidence: concurrency-group cancellation (no `concurrency:` block exists in any of the 3 workflow files — confirmed via grep), manual cancellation (these 3 runs' annotations read "The operation was canceled" with no actor attribution, unlike a genuinely separate, unrelated manual cancellation found the same session — `stage.yml` run `33760400897`, 2026-09-03, whose annotation explicitly reads "The run was canceled by @akashrn1801"), and runner/infra failure (no infra error anywhere in the logs; the kill sequence is GitHub's standard timeout-cleanup, not a crash).

Root cause of *why* runtime grew past 6h: `sandbox.yml`'s `detect-tests.sh` correctly (by design, Rule 1) escalated to the full `--grep @regression` scope (499 tests) after this session's `src/core/BasePage.ts` change — the same scope `qa.yml` already runs every push, and `stage.yml`'s own full, unfiltered 515-test suite is even larger. All three had simply outgrown the 6-hour ceiling through organic suite growth (453→515 tests since 2026-09-02) plus this session's own core-file change legitimately escalating sandbox's scope.

**Real fix designed 2026-09-09 (sharding, not a timeout-value change) — pending review, not yet merged as of this writing.** `qa.yml`, `stage.yml`, and `sandbox.yml`'s own full-regression-escalation path are redesigned to split test execution across a 4-job GitHub Actions matrix using Playwright's native `--shard=N/4`, each shard running independently well under the 6-hour ceiling (~90-93 min/shard measured/projected today, ~2.8-4x margin, with headroom checked against projected growth of +200 tests/year), with a final `merge-and-report` job using `npx playwright merge-reports` to consolidate all shards back into ONE report/notification — not four disconnected ones. Sharding was chosen over raising `timeout-minutes` further (proven ineffective above), over `--workers` tuning (blocked by the documented `--workers=1` credential-race risk — see this file's "Concurrent-Worker Credential File Race" section in `CLAUDE.md`), and over a self-hosted runner (a materially bigger infra change, not needed once sharding alone clears the ceiling with large margin). One confirmed, evidence-based limitation of this design: `allure-playwright` is deliberately excluded from the merge step — a real 2-shard local test reproduced a `TypeError: Cannot read properties of undefined (reading 'outputDir')` thrown from `AllureReporter.onConfigure` on every `merge-reports` invocation (non-fatal, exit 0, `allure-results/` still gets populated despite the error, but shipping a reporter with a confirmed internal error for a directory none of these 3 GitHub Actions workflows currently upload/generate/consume anyway was judged not worth it — see `merge.config.ts`'s own WHY comment). **Mandatory post-merge follow-up:** the first real sharded run on each branch needs to be watched to confirm shards actually distribute the suite evenly, all complete independently, and the merged report's total pass/fail count matches what an unsharded run of the same commit would show — none of this has been verified via a real GitHub Actions execution yet, only via local reproduction of the individual mechanisms (`--shard=1/1` confirmed a true no-op; `--reporter=blob,list,<custom-reporter-path>` confirmed to correctly produce a blob file, live console output, AND run the custom reporter, all three at once; a real 2-shard merge confirmed correct combined pass/fail totals for html/json).

**A related, independently-confirmed incident from the same 2026-09-08 cancellations, fixed regardless of the sharding work above:** the killed sandbox run's "Sync run history"/"Send notification email" steps ran during GitHub's post-cancellation grace period and picked up a stale, degenerate `results.json` left behind by this same script's own `TEST_COUNT=$(npx playwright test --list ...)` probe — `--list` goes through the exact same JSON reporter as a real run and writes a full report with every listed test marked `skipped` and a near-zero duration, confirmed via live local reproduction. This produced a real, sent email (sandbox Build #167, commit `f22653a2`) falsely headlined **"✅ ... PASSED"** for a run where 0 of 499 tests ever executed — traced to `ReportParser.ts`'s `status` computation (`failed > 0 ? 'failed' : flaky > 0 ? 'unstable' : 'passed'`, with no floor-check for whether anything actually ran) and `EmailTemplate.ts`'s `subject()` independently re-deriving its icon/text from raw `report.status` instead of the unified `computeOverallVerdict()` the 2026-08-24 banner-vs-summary-disagreement fix was built to route both through. **Fixed** (implemented in the working tree 2026-09-09, pending the same review/merge as everything else in this entry): `ReportParser.ts` now emits a genuine `'no-tests-executed'` status whenever `total > 0 && passed === failed === flaky === 0`; `AutomationHealth.computeOverallVerdict()` checks this first and returns an honest `🚫 No Tests Executed` / `blocked` verdict instead of ever reaching a "clean run" framing; `EmailTemplate.subject()` now reuses the identical `ctx.health`/`ctx.verdict` fallback `html()` already uses, closing the whole subject-vs-body disagreement class rather than just this one instance of it. Verified live: `tsc --noEmit` clean, and a direct re-run of the real degenerate report shape through the actual parser/verdict/template code now correctly produces `status: no-tests-executed` / `verdict: blocked` / `SUBJECT: ❌ ... NO TESTS EXECUTED`. **This also means the sandbox verification for the `feature/fix-main-failures-and-empty-field-hide-20260907` branch was never actually valid** — run #167 is the only sandbox run for that commit and it never completed; qa/stage were pushed ~23-25 minutes later and both were independently killed by the same 6-hour ceiling per this section's own evidence above. None of sandbox, qa, or stage validated this branch — all three need a genuine re-run once the sharding fix above is reviewed and merged.

**Deferred cleanup, not fixed now:** while building `scripts/merge-misc-errors.ts` for the sharding fix above, `syncHistory.ts`, `notify.ts`, and `MiscErrorReporter.ts` were confirmed (via `eslint`) to still use `console.*` directly instead of the shared `logger` util (30+ combined warnings) — `merge-misc-errors.ts` itself was switched to `logger.*` on request, but these 3 pre-existing files were deliberately left untouched as out of scope for this fix; migrate them in a future cleanup pass so this doesn't get silently forgotten now that it's known.

### Dynamic CI duration-estimate feature — built 2026-09-04, directly motivated by the above

A historical-data-driven duration estimate replacing every workflow/Jenkinsfile's own hardcoded (and, per above, sometimes wrong) comment. `RunHistoryRecord` gained an optional `workers?: number` field (backward-compatible, verified by tracing every consumer); `computeDurationEstimate()` averages only matching `branch`+`workers` records, returning an honest "insufficient history" reason rather than ever guessing.

**GitHub Actions (all 6 workflows) — implemented; logic verified against real live history data, run locally, not via an actual GitHub Actions execution** (nothing has been pushed). **Jenkins (all 5 Jenkinsfiles) — implemented and structurally reviewed, but NOT YET CONFIRMED via a live run.** Direct evidence found Jenkins' `history:sync` had never successfully authenticated for any of its 5 pipelines (zero `runSource: "jenkins"` records anywhere, no credential ever wired) — same root cause as the earlier GitHub Actions ledger bug, never diagnosed for Jenkins until now. Fixed by binding a Username/Password credential (`github-credentials`) via `usernamePassword()`, never Groovy-string-interpolating the secret into a command. No Groovy/Jenkins tooling exists in this session's environment — verification was structural (brace/quote balance checks, full manual re-read) only, explicitly **not equivalent to a real Jenkins execution.**

**Mandatory post-merge follow-up, not to be silently closed:** once any Jenkinsfile actually runs with these changes, check that run's console log for the `estimate-duration`/`history:sync` steps (no Groovy errors, no credential-binding failures) and check `history/<env>.jsonl` for a new `runSource: "jenkins"` record. **If either check fails, the credential wiring needs to be revisited before being trusted.**

A real bug was caught during this same verification pass, before any live run: the main `Jenkinsfile`'s new estimate-duration call originally hardcoded `ENV=prod`, silently wrong for any other branch reached via the manual-trigger escape hatch — fixed to rely on the same `.env`-file convention the existing `history:sync` call already correctly used.

### Jenkins `Jenkinsfile` branch-to-test-selection bug — separate, pre-existing, fixed (found 2026-09-04)

**Confirmed real, pre-existing (`git blame`: authored 2026-07-01, two months before this session), NOT caused by anything this session touched.** The root `Jenkinsfile` is the only Jenkins job actually wired up (the 4 side-Jenkinsfiles have likely never executed) — and its only branch-conditional test-selection logic was `isProd = env.BRANCH_NAME == 'prod'` with everything else falling through to the full, unfiltered suite. `stage`/`main` happened to look correct by coincidence (full suite is correct for them too); **`dev`/`qa`/`sandbox` were genuinely wrong**, silently running the full 453-test suite instead of `@smoke`/`@regression`/a selective subset for ~2 months.

**Fixed:** replaced with a proper if/else-if chain matching each branch's exact GitHub Actions equivalent — `dev`→`@smoke`, `qa`→`@regression`, `prod`→`@prodSafe` (unchanged), `sandbox`→the real dynamic `detect-tests.sh` output (previously computed but never consumed — a separate pre-existing dead-code gap, now wired in with a defensive `@smoke` fallback), `stage`/`main`→unchanged full suite. Scoped to test-selection only — workers count and timeout formula untouched, confirmed via diff.

**Verification status — honestly capped, same reason as the duration-estimate feature (nothing pushed, Jenkins cannot see this yet):** verified via a full fresh re-read of all 6 GitHub Actions workflows to build the ground-truth comparison table, a complete manual line-by-line re-read of the modified stage, and structural balance checks — **not equivalent to a real Jenkins execution.**

**Mandatory post-merge follow-up, applies to both this fix and the credential/duration-estimate wiring above:**
1. Trigger a manual Jenkins run against `dev` — confirm `@smoke` is actually used and it finishes in minutes, not hours.
2. Trigger a manual Jenkins run against `qa` — confirm `@regression` is used.
3. Check `history/qa.jsonl` (and `history/staging.jsonl` for sandbox/stage) for a new `runSource: "jenkins"` record, and check both console logs for the `estimate-duration`/`history:sync` steps — no Groovy errors, no credential-binding failures.

**If any of these checks fail, this needs an immediate hotfix on `dev`, not a "known issue" left open indefinitely** — both fixes directly affect what tests run on every branch including prod/main, and neither has been confirmed by a real execution as of this writing.
