# Known Issues & Investigation History (full detail)

Imported from `CLAUDE.md`. See that file for the condensed "Critical / Do Not Touch" summary and the rest of the standing rules.

This is the accumulated, evidence-based record of every significant bug class, architectural fix, and investigation conducted on this codebase — consolidated from the former `.claude/known-issues.md` and `.claude/INVESTIGATION_LOG.md`. For confirmed **Kylas application** bugs specifically, see `APPLICATION_BUGS.md` — this file covers everything else: code bugs found and fixed, architectural overhauls, and flakes investigated but not conclusively resolved.

### Session-expiry recovery — full architectural history

This bug class was independently fixed **five separate times** before the final overhaul below (`click()`/`fill()` 2026-07-09, `navigateTo()` 2026-07-20, `armResponseWaitWithRecovery()`/`withSessionExpiryRetry()` 2026-07-20, then the two gaps below). A full-codebase grep eventually found **80 further raw `expect(...).toBeVisible/toHaveText/toHaveURL` calls** written directly in module page objects (Contacts 18, Companies 19, Deals 23, Leads 12, CallLogs 4, Tasks 3, Quotations 1) — every one silently unprotected. This is the exact anti-pattern where per-method retry wrapping keeps getting forgotten by new code — the same shape already solved once before via `src/utils/navigation.ts`'s `safeWaitForURL()` consolidation (three independent bare-`page.waitForURL()` bugs, each rediscovered only after a confusing CI failure).

**Phase 1 — verified against the real app, not assumed.** No cookie-based session at all — clearing `localStorage` alone immediately redirects to `/signIn`. Login is `PUT {apiBaseUrl}/users/login`, response `{token}` — a JWT stored in `localStorage.token`; every API call sends `Authorization: Bearer <accessToken>` from inside the decoded payload. The JWT's `refreshToken` field has no usable refresh endpoint — three plausible candidates all returned bare, header-less 400s. A concurrent re-login with valid credentials returns the identical `accessToken` and doesn't invalidate the first — concurrent logins are server-side safe.

**Phase 2 — a `page.route()` network-interceptor design was built, verified to partially work, then deliberately abandoned (not shipped broken).** The core mechanism worked (401 detection, dedup, retry all correctly), but a broader regression sample showed `route.fetch()` itself — even on requests nowhere near a 401 — broke completely unrelated saves with generic `HTTP 400 "Unexpected error occurred!!"` errors. A/B tested decisively: no interceptor = clean; interceptor registered but `route.continue()`-only = clean; `route.fetch()` actually used = breaks. Root cause (well-evidenced, not fully proven): replaying via Playwright's Node-side networking instead of the browser's native stack gets flagged by the backend. **`route.fetch()`-based interception is unusable for this app.** Immediately neutralized before doing anything else.

**Phase 3 — what was actually built: one shared combinator, not 80 hand-rolled fixes.** `BasePage.withSessionExpiryRecovery<T>(fn: () => Promise<T>): Promise<T>` — capture the URL *before* the call (the page may already be on the failure page by catch-time); on catch, check registration + expiry-page condition; if matched, recover once and retry `fn()`; otherwise rethrow unchanged. All 80 raw call sites became one-line changes; `BasePage`'s own generic helpers (`waitForVisible`, `waitForHidden`, `waitForUrl`, `assertVisible`, `assertText`, `assertFormErrorToast`) now route through it too.

**Phase 4 — a second real gap found via the combinator's own verification, fixed the same way.** Deliberately reproducing `withSessionExpiryRecovery()` (corrupt the token, force a fresh render) surfaced a distinct **"Forbidden"** client-rendered bootstrap page with the URL completely unchanged — a state `isSignInUrl()`'s URL-only check can never recognize. A mid-session expiry redirects to `/signIn`; a fresh page load/reload with an already-invalid token hits a different bootstrap-time auth-check code path that renders "Forbidden" instead. Grepped the whole codebase for any test relying on this exact page as an expected RBAC-denial outcome — zero hits, so recognizing it as an expiry symptom cannot mask any real assertion. Fixed by adding `authManager.isSessionExpiryPage(page)` (URL OR body-text check) and updating **all five** gate checks (`click()`, `fill()`, `navigateTo()`, `assertUrl()`, `withSessionExpiryRecovery()`) to use it.

**Phase 5 — a proactive layer added on top: prevent the failure, not just recover after it.** `AuthManager.ensureFreshSession(page, role)` reads the JWT's own embedded absolute expiry (`payload.data.expiry`) and proactively refreshes via a fast headless login if less than **10 minutes** remain — grounded in this codebase's own timing data (max observed heaviest-flow duration ~2.1 min, so 10 min gives ~5x margin while being ~1.6% of the token's ~10.4h lifetime). Wired into `fixtures/index.ts`'s `createRolePage()`. Deliberately never uses `route.fetch()`/request-replay, so it can't hit the Phase 2 rejection.

**This mechanism's first real-CI execution (sandbox run `30286886093`, 2026-07-27) exposed a much more mundane bug than the elaborate theories initially suspected.** `loginHeadless()`'s comment asserted `config.apiBaseUrl` "already includes `/v1` for every environment" — true only for the local `.env` copy, never cross-checked against the CI secret backing the same variable. 64 of ~90+ sandbox failures and 98 of a separate qa-regression run's failures were the identical `HTTP 404 from /v1/users/login`. Confirmed mechanistically: `curl -X PUT https://api-qa.sling-dev.com/v1/users/login` → 401 (real route, correct); the identical call without `/v1` → 404. **Fixed** via `AuthManager.getLoginUrl()`, later consolidated into the shared `config.buildApiUrl(path)` (see below) — normalizes the base URL regardless of which shape a given environment's secret happens to use.

**SECOND occurrence of the exact same normalization bug, found one day later — in a call site the first fix's own ripple-check had already grepped and missed.** `DealsPage.fetchCurrentDealApiData()` built its URL as `${config.apiBaseUrl}/deals/${id}` — same unqualified-`/v1` assumption. Direct proof: `GET {base}/v1/deals/430321` → 200; `GET {base}/deals/430321` → 404, silently swallowed by `if (!res.ok) return null` with **zero logging on either branch** — meaning every caller of this method got false negatives in CI regardless of whether a contact was actually associated. **Root cause of the miss, stated plainly:** yesterday's grep DID surface this exact line, but it was pattern-matched and dismissed as "logging only" without actually reading what it does — finding a match is not the same as understanding it. **Fixed by consolidating into one shared canonical utility** — `config.buildApiUrl(path)` — instead of a third independent copy, mirroring the `safeWaitForURL()` precedent. Logging added on both branches, closing the silent-failure gap. This bug's downstream effect on the Deal Contact-persistence investigation is covered in `APPLICATION_BUGS.md`'s "Investigated but not confirmed" section (that entry was itself later retracted 2026-08-11 as no-longer-reproducible — see below).

### ID-capture and response-matching bug class

**Confirmed false-positive bug, found in 3 places, fixed:** `DealsPage.captureDealIdFromResponse()`, `CompaniesPage.captureCompanyIdFromResponse()`, and a third instance in Deals' "add quotation from deal panel" flow all used a bare `.includes('/deals')`/`.includes('companies')`/`.includes('/quotations')` substring match with no version prefix — each could match an unrelated background analytics/reports POST (confirmed live: `/v4/reports/deals`) that raced ahead with no `id` field, silently capturing `null` and throwing a false "save failed silently" error despite a genuine success toast (e.g. "Quotation created (Quotation ID: 8794)"). Fixed all three by requiring the real `/v1/<module>/` path and excluding `/reports/`. **If you add a new `captureXxxIdFromResponse()` anywhere, require the versioned path from the start.**

**Session-expiry recovery for `waitForResponse()` ID-capture — two real bugs found via deliberate reproduction before trusting the design:**
1. **A race in the original flag-based coordination.** `armResponseWaitWithRecovery` used to set a WeakMap flag only *after* fully awaiting its own recovery (10+ seconds). For methods shaped like `LeadsPage.saveLead()`, code reacting to the same failure much faster (`assertNoFormErrors()`, ~1.5s) could throw and propagate before the flag was ever set — reproduced live via a synthetic `page.route()`-injected 401. Fixed by redesigning `armSessionExpirySignal()` to expose a synchronous `hasFired()` boolean, toggled the instant the event arrives.
2. **A competing-navigation race in `tryRecoverSessionForPage()` itself.** The app's own frontend can independently fire its own client-side redirect to `/signIn` in reaction to the same 401 — reproduced live as `net::ERR_ABORTED; maybe frame was detached?` immediately after the injected 401. Fixed with a single bounded retry keyed on this exact signature.

`withSessionExpiryRetry(workflowFn, description)` wraps an entire top-level workflow method and retries the whole thing once on detected expiry — **deliberately NOT applied** to share/reassign/clone/add-from-panel methods (a dedicated 4-agent investigation confirmed these assume the page is already on a specific entity's detail view; a blind retry after a recovery navigation would fail worse than today).

### Navigation-drift gap across 6 modules — confirmed and fixed (2026-07-27)

**The confirmed gap:** every `waitForXDetailsPage()`/`waitForListReady()` across Deals, Companies, Contacts, Leads, Tasks, and Quotations independently duplicated the same shape — `waitForUrl()` (resolves the FIRST moment the URL matches, no guarantee it stays matching) followed by an entity-GET wait wrapped in `.catch(() => null)`. Under heavy concurrent load, the app's own client-side router can bounce away from the just-matched URL before that GET ever fires — the silent catch swallowed this completely. The real failure then surfaced much later as a generic element-not-found timeout with zero diagnostic signal.

**Full-codebase sweep before fixing:** confirmed identical shape in Companies/Contacts/Leads (list + detail variants) and Tasks/Quotations (detail variant only). Checked Meetings/Call Logs — confirmed **no** matching gap.

**Fix:** two new shared `BasePage` methods, `waitForEntityDetailPage()`/`waitForEntityListPage()` — one canonical reload-and-retry implementation instead of 6 independently-drifting copies. Every module's own method refactored to a thin wrapper. **Fast-failure guarantee, explicitly verified:** absolute theoretical worst case ~190s (~3.2 min), a ~60% reduction from the previous unbounded 480s, always with a clear diagnostic message instead of a bare generic timeout. For the actual bug pattern (URL matches, then redirects away), realistic time-to-clear-failure is ~35-40s.

**A second, genuinely different bug found along the way:** re-verifying after the navigation-drift fix, the Quotation-permission test still failed, but now in 2.0 min with a clear validation error instead of an 8-minute silent timeout. Traced to `DealsPage.getAssociatedContactId()`/`getAssociatedContactName()`/`getAssociatedCompanyName()` having **no explicit timeout at all** (this codebase never sets `actionTimeout`, whose real Playwright default is 0/unbounded). Fixed by adding an explicit 10s bound to all three reads.

**Zombie session-expiry listener race — the same investigation's third finding.** `waitForEntityListPage()`/`TasksPage.waitForListReady()` raced `armResponseWaitWithRecovery(...)` against a plain table-visibility wait via `Promise.race` — which never cancels its losing branch, only abandons it. Since the table-visible branch almost always wins first, the abandoned expiry-listener-arming promise could keep listening for up to 60s after the calling method returned, ready to fire a wrongly-targeted recovery if a real expiry landed in that window — confirmed via a real full-suite run where TWO unrelated recovery attempts fired within 3ms of each other for calls that had already returned 27-54 seconds earlier. **Fixed** by swapping in a plain, listener-free `page.waitForResponse()` for the race's response branch — 100% behavior-preserving for list-readiness, only removing the dangerous unused side channel. Structural fix, not a forced-reproduction one: several deliberate repro attempts confirmed the hazard exists by construction but didn't cleanly reproduce the exact 27-54s survival window on demand.

### Random-option-pick dropdown hangs — generalized fix across 5 modules

`DealsPage.selectFirstOptionFromDropdown()`'s first fix (bounded 15s retry on a stuck option click) covered one symptom but not the bug class: 6 other call sites across `DealsPage.ts` (product row, associated contact), `TasksPage.ts`, `CallLogsPage.ts`, `MeetingsPage.ts` picked a random react-select option via the same unbounded `textContent()`+`.click()` shape. Also found: retrying the *same* index is sometimes futile — one specific index can be persistently non-actionable while every other index works fine. Both fixed via one shared `BasePage.selectRandomOptionWithRetry(options, description, opts?)` — 3 attempts, each bounding read+click to 15000ms, each **re-rolling a fresh random index**. **If you add a new "pick a random option from an open react-select menu" call site anywhere, use this shared method.**

`DealsPage.fillDealForm()`'s associated contact/company selection is **random by design**, a deliberate 2026-07-05 fix for a CI-hang problem — not itself a bug, but it means any new Deals test where ownership matters must pass `associatedContactName`/`associatedCompanyName` on `DealData` instead of relying on the random pick.

`BasePage.fillSearchAndWaitForOptions()` retries a react-select search on a transient DNS/network blip only — `TRANSIENT_NETWORK_ERROR_PATTERNS` is a deliberately narrow allowlist (DNS/connection reset/refused/timeout/unreachable), explicitly excluding real HTTP 4xx/5xx and routine `ERR_ABORTED`. **Do not widen this allowlist without the same live-evidence bar.**

### Locator/timing fixes catalog (by module)

- **Lead multi-select "chip drop"** — `selectRandomFromMultiValueReactSelect()` used to silently drop a previously-landed chip. Root-caused via `document.elementFromPoint()` instrumentation: the "reopen menu" click landed on the control div's center, which drifts onto a previous chip's remove icon as chips wrap onto multiple lines. Fixed by targeting the control's own `<input>` child instead.
- **Company Phones field collision (Lead)** — `phoneInput = () => page.locator('input[id*="input_phone_0"]')` was a loose substring match that also matched Company Phones' first entry. Company Phones was removed; the underlying fix (switching to the exact `name="phoneNumbers[0]"` attribute) was applied to all three modules with the identical fragile pattern (Leads, Contacts, Companies), not just Lead.
- **GPS trigger is section-scoped, not page-wide** — Lead's form has two "Get GPS Address" triggers with identical visible text; an unscoped `page.getByText(...)` throws a Playwright strict-mode violation. `BasePage.getFormSectionContainer(sectionHeading)`/`getGpsAddressTrigger(sectionContainer)` scope the click and throw a clear error on ambiguity instead of silently falling back to `.first()`.
- **`escapeRegExp()` duplicated privately across 5 page objects** — byte-for-byte identical in `DealsPage.ts`, `CompaniesPage.ts`, `ContactsPage.ts`, `LeadsPage.ts`, `TasksPage.ts`. Moved to one shared `protected BasePage.escapeRegExp()`.
- **`QuotationsPage.ts` per-row DOM-read loop** — three methods looped every list row with an individual `.innerText()` call. Replaced with one batched `allTextContents()` call — 6.4×–38.2× faster across QA/Staging/Prod in 9 timing runs. **If you find a `for (...) { locator.nth(i).innerText() }` pattern anywhere else, this is the fix.**
- **`CallLogsPage`'s Associated Deal field** — intermittent "zero options after retries" was a **data-linkage** problem, not a search-index-lag race: `ensureOwnedDealExists()` created the deal with no `associatedContactName`, so it got linked to a random, unrelated contact per the random-pick behavior above — no amount of retrying could find a deal never actually related. Fixed by threading the contact's name through to `generateDealData({associatedContactName})`.
- **`fillEditForm()` asymmetry (Lead vs. Contact) is real and intentional, not a bug:** Lead's `fillEditForm()` does NOT update Timezone, Country, or the 5 Professional fields (deliberately scoped to firstName/lastName/Salutation/Requirement/custom-fields only); Contact's DOES re-fill Timezone/Company. Open decision for a maintainer: extend Lead's edit coverage (riskier — react-select re-selection on a pre-filled form is exactly where subtle bugs live) or accept as create-only. Currently left as create-only.

### CL21/CL23/TC23/TC21/TK24/CL22/CL24 — sandbox CI code-bug investigation (2026-08-02/03)

Sandbox CI run `30706489847` (commit `26593ea`) surfaced 7 test failures. All 7 were **code bugs**, not app or environment issues, each re-verified live before fixing:

- **CL21/CL23 (shared root cause) — React Select dropdown pointer-event interception.** `CallLogsPage.fillPhoneNumber()`'s `phoneControl.click()` failed with "Target element... intercepts pointer events. After 209+ click retries..." — the dropdown menu was open from a prior interaction. Fixed with `openDropdownById()` + `force: true` (JS event dispatch avoids the interception). CL23 additionally had a second, distinct interception during `fillEditForm()`'s "Other Details" tab click — fixed the same way (`force: true`). Both verified 3/3 stable after fix.
- **TC23 — hardcoded `waitForTimeout(200)` calls stacking cumulatively** in `BasePage.selectDateTimeCustomField()` (3 calls after each time-picker column click), pushing an 8.1-minute test 6 seconds over the 480s timeout. Fixed by removing all three blind waits and replacing with one condition-based wait for `.rc-time-picker-panel` to become hidden after pressing Escape — the same pattern already proven in `MeetingsPage.fillTimePicker()`.
- **CL22 — unscoped "Log a call" button locator** (`button.btn.btn-primary` filtered by text, page-wide) could match the wrong button. Fixed by scoping with `.first()`.
- **CL24 — validation-error locator pattern too narrow**, missing a broader error-toast class already used elsewhere in the same file. Fixed by adding `.alert-danger:visible` to the selector.
- **TC21 — missing session-expiry recovery** on a tab click in `assertTaskCustomFieldsOnDetail()`. Fixed by wrapping in `withSessionExpiryRecovery()`.
- **TK24 — unbounded `expect(timeInput).toBeEnabled()`** could hang the full test timeout if the time input never enables. Fixed with a non-blocking `isEnabled({timeout:5000}).catch(() => false)` + warning log — fails fast with diagnostics instead of hanging.

**Call Log custom-field validation (2026-08-03) — confirmed NOT a bug.** The backend simply does not enforce character-limit/format validation on Call Log custom fields yet (unlike Meeting/Quotation/Task, which do). The corresponding test is `test.skip()`-ed with a comment pending future backend work — see `tests/ui/call-logs/call-logs.spec.ts` near the CL24 test.

### 5 sandbox failures (CL39, CL32, CL33, D28, Q-detail-verify) — full root-cause investigation (2026-08-09)

Sandbox CI run `31269450132` (2026-08-08, commit `95e602e`, confirmed real `--workers=2`/298 concurrent tests — `sandbox.yml` dynamically sets `WORKERS=2` whenever the selected test count exceeds 50) surfaced 5 test failures across Call Logs, Deals, and Quotations. All reproduced live on staging before any fix (no guessing):

- **CL39 (`call-logs.rbac.spec.ts:698`, "restricted user can update call log custom fields") — a real, deterministically-reproduced test-data bug, initially mis-suspected as a "date-time picker hang."** The actual failure (`Test timeout of 480000ms exceeded` + `locator.click: Target page, context or browser has been closed` inside `CallLogsPage.selectFromDropdown()`, stuck on `1_31_input_callType`) had nothing to do with any date/time widget — `fillCallType()` runs before custom fields in `fillEditForm()`. Root cause: the test generated `originalData` and `updatedData` via two independent `generateCallLogData()` calls with no `entityType` pinning — `entityType` (Lead vs Contact/Deal) determines which `callType` option text is valid ("I called the Lead"/"Lead called me" vs "I called the Contact"/"Contact called me"), and `fillEditForm()` never changes entity type on edit. An independently-randomized `updatedData.entityType` had a real chance of producing a `callType` option that simply didn't exist in this call log's dropdown, and `selectFromDropdown()`'s option click had **no timeout at all**, hanging the full 480s. **Confirmed via deliberate deterministic reproduction** (forced `originalData.entityType: 'Lead'`, `updatedData.entityType: 'Contact'`) — identical failure signature. **Fixed**: pinned `updatedData`'s `entityType` to `originalData.entityType` in the test, plus added explicit `waitFor`+`timeout` bounds to `selectFromDropdown()`'s option click (and the sibling `fillEntityType()`'s identically-unbounded option click, found via the same locator-reviewer pass). Verified 3/3 clean on staging.
- **CL32/CL33 (`call-logs.rbac.spec.ts:508`/`:530`, admin-role "Log a Call form did not open after 5 attempts") — confirmed NOT a code bug in `openLogACallForm()`'s retry logic; strongly correlated with a genuine Kylas app-side JS race under concurrent access.** Reproduced CL32 cleanly 2/2 in true single-worker isolation (opens on first attempt both times) — ruled out a straightforward logic bug. Deliberately re-running the full spec file with `--workers=2` on staging (matching the real failing CI config) captured a live app-side `TypeError` (`Cannot read properties of undefined (reading 'content')` inside the Kylas app's own minified `openCallLogForm` function) via `ErrorCollector`, correlating with a genuine failure in a *different* test at the exact moment two concurrent workers were both interacting with the Log a Call flow — same failure family, same CI conditions, not proven identical to CL32/33's exact occurrence. Full detail and evidence in `APPLICATION_BUGS.md` #1. **Code-side response**: `openLogACallForm()`'s existing 5-attempt reload-and-retry structure was correct and left unchanged (a full reload is the right mitigation for a transient client-side race); added capture-and-log of the real caught error + current URL on every retry attempt, and the final exhaustion error now surfaces the last real error instead of a bare "did not open" message — so a recurrence is immediately diagnosable instead of a mystery flake.
- **D28 (`deals.rbac.spec.ts:1174`, "restricted user can clone their own deal") — a real, previously-undetectable gap: `cloneDeal()` never verified it was actually looking at the Clone modal.** `#editEntityModal` is a shared container reused for Edit/Clone/Add-Contact/Add-Quotation on Deals; two sibling flows in the same file (`addContactToDeal()`, the Add-Quotation flow) already guard against this by checking `.modal-title`, but `cloneDeal()` never did — it just waited for the container to be visible, then polled the Name field for up to 10s waiting for "Copy" to appear, which is what actually failed in the sandbox run (`Received string: "Deal-Frozen Marble Cheese-5bvR"`, no "Copy", after 23 polls). **Fixed**: added the same `.modal-title` → "Clone Deal" check (live-confirmed this session) the sibling flows already use, so a wrong/stale modal now fails immediately with a clear cause instead of a confusing 10s-later "Name field not pre-filled" error; also widened the Name-field pre-fill timeout from 10s to 20s as a real, evidence-based safety margin (the original "8/8 clean" baseline for that wait predates this run's confirmed real `--workers=2` load). Both new `expect()`s wrapped in `withSessionExpiryRecovery()` per rule 3 (a gap locator-reviewer caught — the pre-existing Name-field check in the same method had the same gap, fixed at the same time). **Applied the identical hardening to `TasksPage.cloneTaskViaEllipsis()`** (same shared-modal architecture, and previously WEAKER than Deals' pre-fix version — it never checked the Name value at all, meaning a wrong modal there could have silently saved a clone with the unchanged original name and zero error). Both verified 3/3 clean on staging.
- **Quotations detail-page-after-create test (`quotations.rbac.spec.ts:304`) — a real, confirmed code bug: `QuotationsPage.waitForListReady()` never actually checked readiness.** It only did `waitForLoadState('domcontentloaded')` + a blind `waitForTimeout(1000)` — no check that the list or "Add Quotation" button had rendered, unlike `CallLogsPage.waitForListReady()`'s already-proven race between the list container and the header button. Combined with `openCreateForm()`'s `createButton().click()` having **no timeout at all**, if the button took longer than 1s to render under real load (this run's confirmed `--workers=2`), the click waited the rest of the 480s test for a button that either eventually rendered or never did — matching the exact failure signature (`Test timeout of 480000ms exceeded` + "browser has been closed" waiting on `locator('button').filter({hasText:'Add Quotation'})`). **Fixed**: `waitForListReady()` now races the list container against the create button (mirroring `CallLogsPage`'s proven pattern); `openCreateForm()`'s click now goes through the bounded `this.click()` helper. **Swept the same file for the already-documented known-unfixed `fillOwner()` unbounded click** (named in `.claude/reference-patterns.md` §3) and fixed it with the same bounded-click + 3-attempt-retry pattern already proven in `CompaniesPage`/`LeadsPage`'s `openUserShareTypeSearch()`. Verified 3/3 clean on staging. Other named-but-unfixed unbounded-click instances in this file (several random-option pickers) remain unfixed — a large, separately-scoped cleanup, not attempted this session.

**Task 1b — codebase-wide date-time-picker audit (done despite CL39 turning out unrelated, per explicit request to cover the class defensively):** grepped every date/time picker interaction site across all 9 modules. Found and fixed genuinely duplicated, un-fixed blind-wait anti-patterns in `CallLogsPage.selectTimeInPicker()` (4 blind waits between/after rc-time-picker column clicks) and `MeetingsPage.fillTimePicker()` (3 blind waits) — both duplicating a shape already correctly fixed with zero blind waits in `BasePage.selectDateTimeCustomField()` (the TC23 fix above). Also removed the identical redundant-blind-wait pattern from 2 calendar-navigation loops in `MeetingsPage.ts` (date fill + reschedule) and the loop-internal instance in `DealsPage.selectDateInPicker()`/`QuotationsPage.selectDateInPicker()`. **Deliberately left one wait per Deals/Quotations untouched**: a `/* Firefox: wait for calendar open animation to settle */`-labeled wait predating this session, since this project genuinely runs a local `firefox` project (not just chromium) and no live Firefox verification was possible this session — flagged as a stale, undocumented (commit `68e4e3c`, no linked repro) claim worth re-verifying live on Firefox in a future session, not removed on inference alone. Tasks/Leads/Companies/Contacts have no native date/time picker of their own (only the already-fixed shared custom-field methods) — nothing to change there. All fixes verified live via the existing test suite (BasePage's zero-wait pattern was independently re-confirmed working correctly in every verification run this session).

**A 6th, related finding surfaced by this session's own stress-testing, not one of the original 5 CI failures:** re-running the full `call-logs.rbac.spec.ts` under `--workers=2` (to gather evidence for CL32/CL33) caught a *different* test — CL18/"restricted user should update all editable fields..." — failing twice, with two different proximate symptoms each time (the app-side PAGEERROR described above once; a plain `TimeoutError` waiting for `button[data-original-title="Edit"]` to become visible the other time). Root cause not independently confirmed (two different symptoms across two occurrences points at general concurrent-load degradation, not one clean mechanism) — **hardened defensively** by applying the same already-proven reload-and-retry pattern used in `LeadsPage`/`ContactsPage`/`CompaniesPage.assertRightPanelIconVisible()` to `CallLogsPage.clickEditButton()`. The comment is deliberately written to reflect this as analogy-based hardening, not a confirmed fix, per rule 10.

**Final verification (2026-08-09) — the two most important regressions both came back clean:**
- Full `deals.rbac.spec.ts`, single worker: **25/25 passed, 0 failed** (1.2h) — confirms the D28 clone fix and the `DealsPage.ts` date-picker change didn't regress anything else in the file.
- Full `call-logs.rbac.spec.ts` re-run under the **exact real `--workers=2` condition that originally produced CL18/CL32/CL33/CL39**: **24/24 passed, 0 failed** (52.2 min). Every captured background error in this run was an already-documented, unrelated class (call-recording `ERR_BLOCKED_BY_ORB` — see below, this was later root-caused to a test-fixture defect, not an app bug; expected RBAC 404s; one benign navigation-cancellation pageerror) — no recurrence of the `openCallLogForm` `Cannot read properties of undefined` error or the `clickEditButton()` timeout. This is real, load-bearing evidence that the fixes hold under the adversarial conditions that caused the original failures, not just in isolation.

**Call recording `ERR_BLOCKED_BY_ORB` — root cause corrected 2026-08-11, was NOT an application bug.** Previously documented here and in `APPLICATION_BUGS.md` #1 as a confirmed Kylas backend/signed-URL defect. Re-investigated live after the user reported manual recording uploads play back fine: created a real, validly-encoded 5-second MP3 via `ffmpeg` and uploaded it through the exact same "Log a Call" UI flow and field as the automation — it played back correctly (`HTTP 206`, working `0:00/0:05` audio player, screenshot at `logs/evidence/call-recording-real-file-plays-correctly-2026-08-11.png`), through the identical `response-content-disposition=attachment` signed URL construction that blocks the original fixture. The automation's own `src/data/files/test-recording.mp3` is a 40-byte, essentially-empty ID3-only stub with no real audio frames (confirmed via `file`/`xxd`) — this, not the Content-Disposition header, is what Chromium's ORB rejects. **Not a Kylas application bug — a test-fixture defect in this codebase.** Full write-up moved to `APPLICATION_BUGS.md`'s "Investigated but not confirmed" section. **Fixed 2026-08-11:** `test-recording.mp3` regenerated with real audio content (see `APPLICATION_BUGS.md`'s entry for the exact command and verification).

### Credential leakage into plaintext logs — confirmed and fixed (2026-07-20)

`tests/ui/dashboard/login.spec.ts` calls `LoginPage.loginWithCredentials()` with the **real** `QA_ADMIN_PASSWORD`/`QA_RESTRICTED_PASSWORD` — that flowed into a generic `BasePage.fill()` which unconditionally logged the filled value, writing the real plaintext password into every log file from any run touching `login.spec.ts`. Confirmed via grep against 5 historical log files — a long-standing, pre-existing gap. Mitigating factor: `logs/` is gitignored, never committed. `authManager.ts`'s own `_doLogin()` was already safe (fills via a raw locator call, never logs the value) — the leak was confined specifically to `login.spec.ts`'s direct calls.

**Fixed:** `BasePage.SENSITIVE_FIELD_PATTERN` (`/password|passwd|pwd|secret|token|api[_-]?key/i`) + `isSensitiveFieldDescription()` — `fill()` now logs `[REDACTED]` whenever the caller's description matches. Deliberately description-based, not a DOM `type="password"` check (confirmed the one real password-type locator's one call site already passes an accurate description). Deliberately does **not** mask email/username — both admin/restricted emails are `@mailinator.com` disposable test addresses, not real PII. Verified: re-ran `login.spec.ts` after the fix, grepped the resulting log for the real passwords — zero occurrences; `[REDACTED]` appears exactly 3 times as expected. **Still open, not a code fix:** the 5 pre-fix historical log files still contain the real plaintext password on local disk (gitignored, never committed) — rotating the QA credentials was recommended but not yet acted on.

### RBAC test-isolation and app-bug investigations

**Both the Deal Contact-persistence/display bug AND the RBAC company-share-bypass bug (Meeting-creation race), previously cited here as confirmed real Kylas application bugs, were retracted 2026-08-11** — both re-verified live on QA at the user's request and found no longer reproducible (Deal Contact-persistence: a genuine full-reload check showed the Associated Contacts card correctly rendering; the Meeting-creation race: the existing CR9 repro test passed 3/3 clean, though with a caveat — see `APPLICATION_BUGS.md`'s "Investigated but not confirmed" section for both write-ups, including the honest caveat on the Meeting-creation race's re-check not exactly re-creating the original narrow race-timing condition). This section covers only the client-side investigation work that ruled out (or confirmed was NOT) a test-code cause before each was originally accepted as app-level.

**Stage environment confirmed to NOT have the Contact-persistence display bug — which unmasked a separate, genuine, latent test-setup bug in D13b (2026-07-27).** During a zero-tolerance stage verification pass (run explicitly because stage doesn't reproduce the display bug), D13b failed with a completely different signature: the contact link rendered fine, but the test's own ownership assertion failed, because D13b creates its deal with no `associatedContactName`, falling through to the random-pick path — a pre-existing, self-documented risk in this exact test's own setup. It was never triggered on qa only because the (separate) display bug always made the contact link time out first, masking this assertion entirely. **Fixed** (D13b only): create a contact explicitly as the restricted user, then pin the deal to it via `associatedContactName`. Re-verified 5/5 clean on stage; full 12-file zero-tolerance stage regression: 215/216 passed (the 1 failure being this exact D13b occurrence, pre-fix).

**CR13 title correction** — `contacts.rbac.spec.ts`'s combined-permissions test previously claimed 5 permissions ("Note Task Meeting Call Quotation") but only ever shared/verified 4; Quotation is genuinely inapplicable to Contacts' restricted-user view, not an oversight. Title corrected to say "four," named explicitly.

**Contact + Meeting/Company RBAC — corrected premise, not a bug (2026-07-16).** Initially suspected a Contact-side instance of the company-share-bypass bug; corrected — this was genuine, correct RBAC enforcement (sharing a Contact without its associated Company means the restricted user truly cannot access the company, so Meeting creation on it should correctly fail). The test was reframed as a proper negative assertion, plus a new positive counterpart added with both entities correctly shared.

**Lead "Company Lookup"/"Contact Lookup" custom fields (L46/L47 in `leads.spec.ts` — renumbered from L20/L21 on 2026-08-11; L30/L31 in `leads.rbac.spec.ts`, unchanged) — 9 real code bugs found and fixed across 7 page-object files during implementation (2026-07-21/22).** Every fix verified 3x on **each** of qa/staging/prod (9+ runs minimum) before being considered done:
1. Detail-page carousel reset — Lead's "Other Details" is a non-cycling Bootstrap carousel; inactive-slide fields are `display:none` but attached. Fixed with `BasePage.revealDetailCarouselSlideFor()` (direction-aware navigation + token-boundary XPath, since a naive `contains(@class,"carousel")` false-matches `.carousel-item` itself).
2. `CompaniesPage.clickAddCompany()` modal-open race — same click-handler-not-yet-attached class as `DealsPage.cloneDeal()`. Fixed with a bounded re-click retry.
3. `createCompany`/`createLead`/`createContact` transient-backend-400 blind spot — a generic error response with no session-expiry redirect fell through to a false "save likely failed silently" throw. Fixed by classifying the create-POST response by body content (2xx→success; non-2xx generic/empty fieldErrors/any 5xx→retry up to 3x; populated fieldErrors→genuine, never retried). Proven via deliberate `page.route()` injection (4/4 correct behavior) before trusting it on real data.
4. Close-lead toggle visibility flake — measured the real baseline (836-2575ms) before concluding the existing 10s timeout had 4-12x headroom already; fixed with a bounded reload-and-retry targeting rare render delay under load, not a bigger arbitrary number.
5. Share-modal ~9-minute hang across Leads/Companies/Contacts/Deals — an unbounded `userOption.click()`; if it silently didn't register, the next unbounded `.fill()` hung until the outer test timeout. Fixed with `openUserShareTypeSearch()` (bounded click + 3-attempt retry) added identically to all 4 modules.
6. `QuotationsPage.selectFromIsInvalidControl()` — same race as #5, fixed identically.
7. `DealsPage.selectFirstOptionFromDropdown()`'s random-pick on Associated Company — picking a random index from a 25+-option unfiltered list intermittently timed out on all 3 bounded attempts, confirmed live on **stage** (an earlier "qa/prod only" scoping had gone stale once stage's data grew — rule 20 in action). Fixed by batch-reading option text via `allTextContents()`, picking one at random, then typing it to filter down to a single match.
8. `ContactsPage.selectFromContactDropdown()` unbounded-click (6 call sites) — confirmed live hanging the full timeout on **prod**. Fixed identically to #5.
9. `CallLogsPage`'s Associated Deal field — see the Locator/timing catalog above (data-linkage, not search-index lag).

### Test-quality and coverage fixes

- **Dynamic-import factory-function pattern** in `leads.spec.ts`'s delete-lead test made it uniquely vulnerable to a transient module-resolution race during a concurrent unrelated edit. Fixed by switching to a normal static import; grep confirmed this was the only such usage anywhere.
- **Lead's Requirement text field** — a genuine, separate plain text input (internal name `requirementName`), distinct from the section's own heading and from Products/Currency/Budget. It's the *first* field in the section by DOM order, not fourth — filled first to match.
- **Detail-page assertions for Lead/Contact fields** — every field's detail-page container id and rendering format confirmed via direct live DOM inspection before writing the assertion, not guessed.

### Investigated but not resolved — inconclusive or unreproduced (excluded from `APPLICATION_BUGS.md`'s confirmed bar)

- **Unexplained Deals flake (2026-07-06):** logging a Call on a deal shared with the restricted user intermittently fails with a permission error even when sharing is verified correct. Six controlled experiments, every theory disproven by a subsequent experiment, no consistent mechanism found. Two later-discovered client-side bugs could have contaminated the original experiments — downgraded to "uncertain," not disproven. **Do not spend further time isolating this from the Playwright side** without backend/network-level access.
- **`quotations.rbac.spec.ts:380` 8-minute timeout (2026-07-20):** one CI occurrence, `openCreateForm()`'s click has no per-call timeout and no global `actionTimeout` exists in this codebase — explains *why* a stuck click costs 8 minutes, not *why* it got stuck. 5/5 clean local reproductions. This raw-click-with-no-bound shape is the prevailing style throughout `QuotationsPage.ts` — **not a safe one-line fix on this evidence alone; accepted as an unresolved one-time flake.**
- **`call-logs.spec.ts:391` company live-search "no options" (2026-07-20):** one occurrence, notable but unconfirmed correlation with rising swap usage (390Mi→511Mi→683Mi) in the same window. Not fixed — needs recurrence or live-reproduction to settle.
- **`meetings.spec.ts:120` reschedule failure (2026-07-20):** one occurrence, correlated with three `HTTP 500 [GET] /v1/meetings/meeting-invitee/lookup` responses in the same test. Passed cleanly on retry. Classified the same tier as the two above — suggestive, not confirmed.
- **The full run's one genuine terminal failure, `quotations.spec.ts:247` (2026-07-20):** root cause confirmed as a raw TCP/HTTP connection reset (`"Connection has been closed BEFORE response..."`), not a code defect — the code's own handling (re-throwing rather than masking behind the 029003 retry logic) was correct. Became terminal only due to `test.describe.configure({mode:'serial'})`'s full-file-retry semantics cascading from an earlier test's own transient failure in the same window. **Fixed defensively anyway:** a narrow, bounded retry (`TRANSIENT_CONNECTION_RESET_PATTERN`) added ahead of the existing 029003 classification, verified via deliberate `page.route()` injection (3/3 clean, exhaustion path throws correctly after 2 retries).
- **`tasks.rbac.spec.ts:69` edit-modal hang (2026-08-03 area):** 0/5 local reproductions; root cause of this specific occurrence not confirmed. Defensively hardened based on code review (rule 10) — `selectReactSelectOption()` now waits for the react-select menu to actually close after an option click, matching the same "wait for a real signal" discipline proven for `DealsPage.cloneDeal()`.
- **HTTP 500 on meeting creation (2026-08-03) and Deals RBAC Task-permission 8.2-min timeout (2026-08-03):** both formally classified **INCONCLUSIVE** after an evidence-gap analysis — pass-in-isolation/fail-in-suite pattern suggests environmental degradation, but a code-side cumulative effect (RBAC permission-check caching, N+1 query behavior under accumulated test data) could not be ruled out for lack of response-time/session-state instrumentation. See `APPLICATION_BUGS.md`'s bottom section for the short version.
- **Profiling hypothesis disproven, do not re-attempt without new evidence (2026-07-17):** 2.4 hours (37%) of a 6.5h/272-test run sat in the gap after "Creating authenticated browser context." Hypothesized `waitUntil: 'domcontentloaded'` was unnecessarily strict. Tested live: isolated timing showed 3.8-5.2s regardless of `waitUntil` value, nowhere near the observed 22-25s gap. Real cause is almost certainly genuine QA-environment load accumulation (consistent with "QA data grows unboundedly," and the host's own swap being 100% full during the same run) — not a client-side bug.

### Products & Services — confirmed mechanisms and quirks

Everything below is live-confirmed (via DOM inspection, network capture, or direct reproduction), not inferred — see `PRODUCTS_AND_SERVICES_PROGRESS.md` for the full build/investigation history behind each item.

- **CKEditor 5 description field requires calling into its internal data model directly, not the DOM.** CKEditor 5 maintains its own virtual data model separate from the rendered DOM; a plain `.fill()`/`.type()` against the contenteditable region only mutates the visible DOM, not the model that actually gets serialized into the save payload. `ProductsAndServicesPage.setDescriptionViaCkEditor()` reaches the live editor instance directly: `document.getElementById('0_22_input_description').querySelector('.ck-editor__editable').ckeditorInstance.setData(text)`.
- **Units is a genuine multi-select control, but every workflow only ever picks exactly one option from it.** Confirmed live: the control's value-container carries `is-invalid__value-container--is-multi`, selecting an option renders a real removable chip, and the saved record's `units` field is a true array (`"units":[{"id":...,"name":"Kilograms (kgs)",...}]`). Opening the menu requires `ArrowDown` after focus (react-select's async "press Down to open" pattern), not an immediate click-to-open like the single-select Country/Category controls. **This distinction caused a real, CRITICAL-tier incident**: `selectFromReactSelect()` originally had no "clear existing chip(s) first" step — harmless for single-select fields (a new pick replaces the old one automatically) but destructive for Units, where a new pick only ever *adds* a chip, and a react-select menu excludes already-selected options from its own list — meaning a second call targeting an already-selected value finds zero matching options and times out. This corrupted the permanent `adminActive` fixture (id 53242) in production data before being caught, root-caused, and fixed (bounded chip-clear-first logic, mirroring `BasePage.selectRandomFromMultiValueReactSelect()`'s already-proven shape) — see the dedicated CRITICAL INCIDENT section in `PRODUCTS_AND_SERVICES_PROGRESS.md` for the full incident report, approved fix, and verification.
- **Product name uniqueness is enforced live, inline, as-you-type/on-blur — not at save time.** `GET /products/has-duplicates?fieldName=name&value=<value>` fires on Name-field interaction and returns `{"duplicate": true|false}`; `hsnSacCode` is checked the same way. This is a genuinely different validation shape from most other modules' save-time-only server errors — `assertDuplicateNameFieldError()` asserts against this live field-level state, not a save-time toast.
- **There is no edit icon anywhere in the Products & Services UI.** Clicking a list row navigates directly to `/setup/products-services/edit/{id}` — row-click itself IS the "open for edit" action, not a separate button. (This is also why the module has no detail page at all — `edit/{id}` doubles as both the view and edit surface; see `.claude/architecture.md`'s new Products & Services deviations section for the full structural picture.)
- **Deal edit + product add triggers a real "Distribute Equally" reconciliation requirement — not an app bug, a real feature the automation must drive through.** When a Deal's Total changes after part-payments were already configured (e.g. adding a product row during edit), Kylas shows an "unallocated amount" banner (`button.distribute-unallocated-btn`) that must be resolved via a modal (`.installments-modal.distribute-modal`, rendered as a `<body>`-level portal, NOT nested inside the triggering `#editEntityModal` — confirmed live, a locator-reviewer scoping advisory to nest it was disproven by direct DOM inspection) before Save's own click handler does anything at all (confirmed via exhaustive network capture — zero requests attempted client-side until resolved). `DealsPage.handleDistributeUnallocatedAmountIfPresent()` (called from `updateDeal()`) detects and clicks through this automatically; `deals.spec.ts`'s dedicated `D41` test (renumbered from `D39` on 2026-08-11 to resolve a collision with `deals.rbac.spec.ts`'s own pre-existing `D39`) exercises and asserts the full flow explicitly. **This was initially, incorrectly concluded to be "a genuine Kylas application bug, unfixable" after exhaustive headless investigation — the user personally verified in headed mode that it was a real, working feature the automation was simply missing a step for.** Standing lesson, now a hard rule for this session's fix phase: never conclude "app bug" from headless/automated evidence alone — verify live, in headed mode, first.
- **A real, intermittent "element detached from the DOM" race on custom-field date pickers during Deal edit flows — root-caused (partially), fixed, but the exact trigger is NOT fully pinned down.** `BasePage.selectDateCustomField()`'s final day-cell click can fail with a genuine Playwright "detached from the DOM" error, observed in ~50-58% of real Deal create+edit cycles that reach this code path, always on the edit flow's FIRST calendar-driven interaction, never on create. The originally-suspected mechanism ("previous field's calendar didn't finish closing") was **directly disproven** via live `MutationObserver` instrumentation: all of a Deal's `SingleDatePicker` widgets (13-16+, one per part-payment row plus the 2 custom date fields) are permanently mounted and only CSS-hidden — they never add/remove from the DOM, so there is no "calendar closing" race to wait for. A follow-up fix (a 3-attempt reopen-and-retry loop before falling back to the pre-existing typing fallback) was verified via 12 real runs to have a **0% retry success rate** — every one of 7 real occurrences exhausted all 3 attempts; all real recovery came from the already-existing typing fallback, not the new retry logic — proving the condition is sustained across several seconds once triggered, not a momentary blip. **Simplified accordingly to 1 attempt** (cheap insurance, not because retrying is proven to ever help) before deferring to the fallback; re-verified with a second independent 12-run batch, 24/24 total across both batches. The leading (still unconfirmed) theory for the root trigger: `fillEditForm()`'s preceding rapid-fire field fills (~60-120ms apart, far faster than a real user) can leave a React re-render lagging behind, landing unpredictably right as the edit session's first calendar-driven click is in flight — plausible and consistent with all observed evidence, but not proven with a captured smoking-gun mutation burst (two live-capture attempts for that were technically unreliable). **Practical impact:** this is a shared `BasePage` method used by all 9 modules with custom fields — the warning may appear in any of their logs during an edit flow with date custom fields; this is expected, self-healing behavior (the final "date set to:" success line always follows, confirmed correct via a separate persistence check), not a regression. Full incident report (CRITICAL-tier, same rigor as the `adminActive` corruption): `PRODUCTS_AND_SERVICES_PROGRESS.md`.

### Consolidated open items — Products & Services session (2026-08-10)

Every `[DEFERRED]` tag, both CRITICAL incidents, and every other unresolved finding surfaced during the Products & Services module build, gathered into one list per an explicit end-of-session consolidation request. Items already resolved during this same session (verified via real execution, not just claimed) are marked closed and kept here only for historical/pattern-recognition value — everything else is genuinely open, not yet fixed. Full evidence for every item: `PRODUCTS_AND_SERVICES_PROGRESS.md`.

**Two CRITICAL incidents — both resolved and verified this session, kept here as pattern references:**
1. **`adminActive` fixture corruption** — `ProductsAndServicesPage.selectFromReactSelect()` had no clear-existing-chip-first step, which silently corrupted the permanent `adminActive` product fixture (id 53242) via the Units multi-select field. Discovered via a failing restore step in a serial test block. Fixed (bounded chip-clear logic) and the live fixture itself was restored via an explicitly user-approved read-modify-write PUT, independently re-verified via a fresh GET diff.
2. **DOM-detachment date-picker race** — `BasePage.selectDateCustomField()`'s day-cell click intermittently failed with "element detached from the DOM" during Deal edit flows, ~50-58% of the time. The originally-suspected mechanism was disproven via live instrumentation; fixed with a 1-attempt-then-fallback approach (a 3-attempt version was tried first and proven to add zero value — 0/21 real recoveries — before being simplified); verified 24/24 across two independent batches. The exact root trigger is still not 100% confirmed (leading theory: automated fill speed outpacing React's render cadence).

**Three grouped naming inconsistencies — flagged, deliberately not fixed, awaiting a scope decision:**
3. **RESOLVED (2026-08-11).** Quotations' `Q22`–`Q27` were duplicated between `quotations.spec.ts` and `quotations.rbac.spec.ts`. Fixed by renumbering the UI file's overlapping range to `Q29`–`Q34` (RBAC file's own `Q22`–`Q28` left untouched). One living doc cross-reference (`CLAUDE.md`, `T22`→`Q22`) updated to `Q29` with a disambiguating note. Verified live: `Q29` and `Q34` both passed. Full evidence: `PRODUCTS_AND_SERVICES_PROGRESS.md` Entry 55.
4. **RESOLVED (2026-08-11) — the original finding's scope was itself incomplete, corrected before fixing.** Re-verification found the real overlap was Leads' `L6`–`L21` (16 labels), not just `L10`–`L21` (12) as originally documented. Fixed by renumbering the UI file's `L6`–`L21` to `L32`–`L47` (RBAC file's own `L6`–`L31` left untouched). Three living doc cross-references fixed (`CLAUDE.md`, `.claude/known-issues.md`'s own Company/Contact Lookup entry, `README.md`). Verified live: `L38`, `L46`, `L47` all passed. Full evidence: `PRODUCTS_AND_SERVICES_PROGRESS.md` Entry 55.
5. **RESOLVED (2026-08-11) — the original claim was wrong; re-verified live before fixing.** `deals.rbac.spec.ts` was NOT actually unlabeled — 23 of its 25 tests already carry real `D10`–`D29` (plus `D13a`/`D13b`/`D24a`) and pre-existing `D39`/`D40` labels from earlier work (documented in `README.md`). Investigating this surfaced a real, separate, self-inflicted problem instead: this session's own new `deals.spec.ts` additions (Distribute Equally, `removeProductRow()` verification) had been numbered `D39`/`D40`, colliding with the RBAC file's own established `D39`/`D40`. **Fixed**: renumbered the UI file's new tests to `D41`/`D42` (the RBAC file's long-standing `D39`/`D40` left untouched), and assigned real labels (`D43`, `D44`) to the 2 RBAC tests that genuinely had no number (both previously used placeholder success messages, not real `D`-labels). All doc cross-references updated.

**JWT/token-lifetime discrepancy — standalone finding, not yet acted on:**
6. **CONFIRMED UNSTABLE — real JWT token lifetime does not appear to be a fixed constant, contradicting even its own earlier "confirmed" value. Deliberately NOT fixed this session.** History, in order: this codebase's original documented assumption was ~10.4 hours. Entry 21 (earlier this session) found `expiresIn: 1182` (~19.7 minutes) live. **A final confirm-only check for this item (2026-08-11), done specifically to verify Entry 21's finding still holds before marking it closed, instead found a THIRD, different value: `expiresIn: 6060–6084` (~101 minutes), reproduced consistently across 4 separate fresh logins in immediate succession** — ruling out a one-off fluke. None of the three values (10.4h assumed, ~20min found, ~101min found) match each other. **This means the two "corroborating" data points cited for the ~20-minute finding do not actually distinguish between a ~20-minute and a ~101-minute lifetime** — a ~40-minute-old storageState landing on sign-in, and a test observing "351s remaining" on an existing session, are both also fully consistent with a ~101-minute lifetime (a session ~95 minutes old would show exactly that remaining-time reading) — so those anecdotes should not be read as confirming 20 minutes specifically, only as confirming the lifetime is shorter than the documented 10.4 hours, which still holds. **The real, load-bearing finding is not a specific number — it's that this value is unstable/environment-dependent and materially shorter than assumed, whatever its current exact figure is.** `AuthManager.ensureFreshSession()`'s proactive-refresh threshold (10 minutes before expiry) was sized against the wrong (10.4h) assumption regardless of which shorter real figure is used — even the more generous ~101-minute reading gives only a ~10x margin, not the assumed ~62x. **Deliberately deferred to a separate future session, now with added urgency**: re-deriving the correct threshold requires first determining WHY this value varies (session type? load? a backend config that changed between investigations?) before any new margin can be trusted — this needs real, dedicated investigation, not a quick guess, and touches shared, foundational `AuthManager` code used by every single test in this codebase.

**Real, open code-level items — genuinely not yet fixed:**
7. **RESOLVED (2026-08-11).** `BasePage.removeProductRow()` was unverified against the live DOM — live investigation on a real Deal found the actual remove trigger is `<i class="fas fa-times pr-2 pt-2 cursor-pointer">`, not the originally-guessed button/aria-label/class-name shape, which could never have matched it. Fixed and verified via a new `D42` test in `deals.spec.ts` (renumbered from `D40` on 2026-08-11 to resolve a collision with `deals.rbac.spec.ts`'s own pre-existing `D40` — real pass, twice, both under its original `D40` label before the rename). Full evidence: `PRODUCTS_AND_SERVICES_PROGRESS.md` Entry 49.
8. **RESOLVED (2026-08-11).** `ProductsAndServicesPage.createProduct()`/`updateProduct()` now accept an optional `customFields` param, threaded through to `fillProductsAndServicesForm()`/`fillEditForm()`. Verified via new `PS16` test — real execution surfaced and fixed two further real bugs along the way: (a) PS16's own raw unauthenticated fetch (fixed via a new shared `BasePage.fetchAuthenticatedApiData()` helper), and (b) a genuine race in `isCustomFieldPresent()` when `updateProduct()` is called with an empty base `changes` object (fixed via a content-readiness wait in `openProductForEdit()`). 3/3 live pass plus a 4-test regression batch, all clean. Full evidence: `PRODUCTS_AND_SERVICES_PROGRESS.md` Entry 50.
9. **RESOLVED (2026-08-11), after two disproven theories and a live network-trace investigation.** D24a (`deals.rbac.spec.ts`) failed with `HTTP 422 "Invalid company summary response."` on Meeting creation. First fix (share the deal's separately-selected Associated Company) didn't help. Second fix (match the Contact's own random Company field to the shared one — a real, genuine bug, correctly fixed and kept) still left the identical 422 firing 4/4 live. **Real root cause, found by inspecting the actual failing `POST /v1/meetings` request body in a headed-mode trace**: Meeting creation from a Deal's panel POSTs `relatedTo: [deal, company]` directly, and the backend's "company summary" check validates the Company's specific `meeting` permission — a bare/empty-permissions share grants generic visibility but not that specific permission. Fix: share both the Contact and Company with `['meeting']` explicitly, not `[]`. Confirmed 3/3 live runs (1 headed + 2 headless), zero failures. Full evidence: `PRODUCTS_AND_SERVICES_PROGRESS.md` Entries 52-54.
10. **RESOLVED (2026-08-11), after two corrected fix attempts.** `quotations.rbac.spec.ts`'s Q7 (`"restricted user should handle inaccessible entity error and retry successfully"`) relied on the stale `QA_ADMIN_DEAL_NAME`. First fix (mirroring Q8's `skipAssociatedEntities`) was live-disproven — a restricted user's deal search is scoped to accessible deals only, not a bare text search. Second attempt (share the deal, leave its random company unshared) worked but was non-deterministic (1/2 runs). **Final fix**: create a dedicated fresh, never-shared Company, pass it via `generateDealData({associatedCompanyName})`, and share the deal itself — deterministic 422 confirmed 3/3 live runs, 0 failures. Full evidence: `PRODUCTS_AND_SERVICES_PROGRESS.md` Entries 51-54.
11. **RESOLVED (2026-08-11).** `.env`'s duplicate `QA_ADMIN_DEAL_NAME` key resolved as a side effect of removing the now-fully-dead `config.deals` block entirely (its only real consumer was Q7, fixed above) — all 6 `*_ADMIN_DEAL_NAME`/`*_RESTRICTED_DEAL_NAME` env vars removed across QA/staging/prod.
12. **RE-OPENED (2026-08-11) — a correction to this session's own earlier work.** This was previously marked "resolved, folded into item 6" on the reasoning that a ~40-minute-old `storageState` landing on sign-in was explained by item 6's then-believed ~20-minute JWT lifetime. **That reasoning no longer holds**: item 6's own live re-verification (same date) found the real, immediately-measured total token lifetime is actually ~101 minutes (reproduced 4/4), not ~20 — and a ~40-minute-old session should still be valid under a ~101-minute lifetime. This anecdote is therefore genuinely unexplained again, not resolved. Not investigated further this session (the original occurrence predates this fix-phase and was already out-of-scope for the batch it was found in) — restored to open status, grouped with item 6's broader "why does this value seem to vary" question for a future dedicated investigation, since both are facets of the same underlying auth-lifetime uncertainty.

**Two new items found via Q7's own fix investigation (2026-08-11) — real, live-confirmed, deliberately NOT fixed (out of scope, span the whole file's pattern, not just Q7):**
16. **`quotations.rbac.spec.ts`'s Q7 test (lines 129-147, re-confirmed live 2026-08-11) has its own hand-rolled "clear the inaccessible entity and retry save" logic that is permanently dead code.** Precise mechanism, confirmed by direct read: Q7 computes `alreadySaved = currentUrl.includes('/quotations/list') || currentUrl.includes('/quotations/details/')` to infer whether the first save already succeeded — but the Quotation create form is an in-place modal, and per `QuotationsPage.saveQuotation()`'s own comment, "the URL never changes on success OR failure." The user never navigates away from `/quotations/list` while the modal is open, so `alreadySaved` is `true` from before the save even runs and stays `true` regardless of outcome — the `if (!alreadySaved)` branch (the clear-and-retry logic that is the entire point of the test) is structurally unreachable, not just usually skipped. Confirmed live: Q7 logged `"Save already succeeded on first attempt — skipping retry"` immediately after a genuinely-thrown 422, a self-contradicting result. **Important distinction, confirmed by direct read of `saveQuotation()` itself (2026-08-11): the shared method is NOT broken** — it already replaced this exact URL-based check with a real signal (form-error check + confirming the modal actually closes), specifically because the URL-based approach was unreliable (its own comment cites the bug this fixed: T13/`quotations.spec.ts:161`). Q7's inline copy was simply never updated to match and quietly drifted back to the broken pattern. **FIXED 2026-08-11.** Root cause of why Q7 had no real signal available: `assertErrorToast()` (`QuotationsPage.ts`) was itself `Promise<void>` and explicitly non-fatal by design ("if no toast, save succeeded on first attempt" — logs a warning either way, never throws) — it already computed the exact boolean a caller needs, it just never returned it. Confirmed only 1 caller existed in the whole codebase (Q7 itself) before widening the signature — safe, no ripple. Fixed by changing `assertErrorToast()` to return `Promise<boolean>` (whether the toast actually appeared) and having Q7 use that directly (`const saveFailed = await qp.assertErrorToast()`) instead of the URL heuristic. **Audit of every other quotation-modal-save call site in both `quotations.rbac.spec.ts` and `quotations.spec.ts`, as scoped**: grepped for the same `currentUrl.includes(...)`/`alreadySaved` pattern — zero other instances found. Q7 was the only occurrence of this anti-pattern in the file; no wider fix was needed once the audit confirmed the scope. `tsc --noEmit` and `eslint` both clean on the 2 touched files.

17. **`assertQuotationInList()`'s search-based existence check can false-positive on this codebase's long-lived, never-cleaned QA dataset — CONFIRMED live on staging 2026-08-17, NEEDS FIX, not just an open observation.** The backend's `/v1/quotations/search` does fuzzy OR-word matching, not exact-phrase matching — searching a real Faker-generated summary returned 10 unrelated pre-existing quotations (some dated over 3 weeks old) that merely share a common filler word, and `retryFindInList()` only checks "any non-empty result," so this assertion can pass without the actual target record existing. Fix would mean tightening the assertion (an ID-based check, or an exact-phrase/quoted search) — affects every test in the file using this same assertion pattern, not just Q7. **Live re-verification (staging, 2 separate `--retries=0` runs of `quotations.rbac.spec.ts`'s Q6, back to back, 2026-08-17):** both runs passed, but both independently reproduced the exact underlying weakness — each run logged `"Search returned 10 row(s) for: <supposedly-unique Faker summary>"` followed immediately by `"Quotation confirmed in list"`, with no check that the actual target quotation was among those 10 results. Both runs capped at exactly 10 (consistent with a default page-size limit, meaning the true fuzzy-match count could be higher than what's even inspected). The test passing both times is expected, not reassuring — a false-positive-prone assertion passing is exactly what this flaw predicts, not evidence against it. **Status: CONFIRMED, NEEDS FIX** (not merely "affects every test," as previously worded) — tighten `assertQuotationInList()` to verify the target record specifically (ID-based check, or an exact-phrase/quoted search) rather than "search returned something."

**4 further items surfaced during the Products & Services build, never actually written up despite the 2026-08-11 consolidation claiming completeness — added here from a direct re-read of `PRODUCTS_AND_SERVICES_PROGRESS.md` (2026-08-17), which is being retired as a scratch file after this pass:**

18. **`BasePage.selectDateCustomField()`/`selectDateTimeCustomField()`'s original calendar-navigation algorithm was elaborate and confirmed failing 100% of the time — replaced with a simpler mechanism already proven elsewhere.** The old version computed the currently-visible month range via a bounding-rect-filtered `page.evaluate()` read, then navigated bidirectionally (forward or backward) based on that. Live evidence during the Products & Services custom-field work showed this always exhausted its full navigation-attempt budget and silently fell back to typing the date directly, on every real custom-field date-fill exercised. **Fixed** by replacing it with the simple forward-only navigation loop already proven never to fail in `QuotationsPage.selectDateInPicker()`/`DealsPage.selectDateInPicker()` (same `react-dates` `SingleDatePicker` widget, same day-cell locator pattern, same label format) — kept the typing-fallback as a defensive safety net, not deleted. Since this is one shared `BasePage` method, the fix applies automatically to all 9 modules with custom fields (`ProductsAndServicesPage`, `TasksPage`, `DealsPage`, `LeadsPage`, `QuotationsPage`, `CompaniesPage`, `ContactsPage`, `CallLogsPage`, `MeetingsPage`) with zero per-module changes. Persistence verified with decisive evidence (not just "no error thrown"): a real deal's create+edit custom-field date fills, cross-checked against a fresh `GET` of the persisted record, matched exactly once the UTC-vs-IST display offset was accounted for.

19. **The native (non-custom-field) date pickers on Deals/Quotations/Call Logs are still independently triplicated, unlike the custom-field version above.** `DealsPage.ts`, `QuotationsPage.ts`, and `CallLogsPage.ts` each have their own separate, private `selectDateInPicker()` method — none goes through `BasePage`, unlike `selectDateCustomField()`/`selectDateTimeCustomField()`, which are genuinely shared. Same underlying `SingleDatePicker` widget, structurally similar navigation-loop shape, but 3 independently-maintained copies. Not consolidated — a real, available cleanup opportunity, not attempted.

20. **A live-confirmed `.custom-month-year-dropdown` control — a direct month/year jump, offering a faster alternative to forward-click calendar navigation — was benchmarked but never adopted.** Exists on the same `SingleDatePicker` calendar widget as every date field in this codebase (a `<li>`-list based month picker + year picker, confirmed live via DOM inspection during the Products & Services custom-field work). Benchmarked head-to-head against the current forward-click method: **~41% faster (585ms) specifically when a real navigation is needed** (jumping to a non-adjacent month) — but NOT benchmarked fairly for the already-fast zero-navigation case, since the benchmark script lacked the same "already showing the right month" short-circuit the forward-click path has, so that comparison (one run showed the dropdown *slower*, 551ms, for a zero-navigation case) shouldn't be read as the dropdown being worse overall. Never adopted anywhere in this codebase (confirmed via grep: zero references to `custom-month-year-dropdown` in any date-picker method) — adopting it would mean writing and testing a new shared code path (with its own short-circuit, its own edge cases e.g. year-boundary crossing, its own locator-review pass), not a one-line change. A real, available optimization opportunity, not attempted.

21. **`npm run test:<module> -- <args>` silently sends passthrough arguments to the LAST command in an `&&`-chained `package.json` script, not the first — confirmed via an isolated reproduction, not assumed.** Every `test:<module>` script in this codebase (e.g. `test:productsAndServices`) is shaped `playwright test <paths> && npm run notify` — running `npm run test:productsAndServices -- --list` (intending to just list tests) actually appended `--list` to `npm run notify` instead, meaning the full Playwright suite executed for real (not just listed) and then correctly chained into a real, unintended `notify` email being sent. Reproduced in isolation with a trivial two-command chained script to confirm this is a genuine npm mechanism, not a one-off fluke. **No fix has been applied** — the recommended fix (documentation-only: never append arguments to a `test:<module>` script, since no real usage in this codebase's history has ever needed passthrough args) was proposed but never actioned; a `--`-safe restructure (splitting into a `-run`-suffixed pair) was considered but rejected as unnecessary blast radius for a mistake that was a one-off usage error, not a real usage pattern. Treat this as a standing gotcha: don't append CLI flags to any `npm run test:*` script in this repo.

**Pre-existing prettier formatting drift — NOT session-caused, deliberately out of scope, purely cosmetic:**
13. `QuotationsPage.ts` — ~20 locations (lines 374, 442, 522-527, 634, 649, 663, 736, 959, 1060, 1086-1099, 1116, 1297-1334, 1373-1423, 1452-1479, 1559-1585, 1842-1868 at time of discovery).
14. `LeadsPage.ts` — 7 locations (945-947, 957, 965, 1102, 1718, 1982-2001, 2151-2167 at time of discovery).
15. `BasePage.ts` — 5 locations (506, 786, 1214, 1853-1862, 1995-2002 at time of discovery).
    Fixing any of 13-15 means a dedicated, scoped `prettier --write` pass reviewed independently of any functional change — mixing it into a functional fix's diff would make that diff impossible to review cleanly.

**Already resolved this session, confirmed via real execution — listed here only so a future read of `[DEFERRED]` tags elsewhere doesn't re-open a closed item:**
- `assertForbiddenOnRestrictedEdit()`'s 403/`00902001` response — now independently verified live (`PS8`, part of the real 12/12 passing run confirming Batch 7).
- `productsAndServices.rbac.spec.ts`'s T10/T11 (now `PS10`/`PS11`) failures — root-caused and fixed via the Distribute Equally discovery; re-verified clean.
- The Batch 7 spec files' final full re-run — confirmed 12/12 passed for real.
- A dispatched subagent's one-time no-git-rule violation (verifying a change via `git diff HEAD~1`) — disclosed immediately in the same session it happened; root cause was an incomplete agent prompt (missing the no-git constraint), not a recurring gap; no further action needed.

**RESOLVED 2026-08-17 — live-verified on staging, 2 separate isolated runs each with `--retries=0 --workers=1` (no retry-masking), no flakiness in any of the 10 runs across these 5 items:**
- **Q7 hardening, both parts (a) and (b) (previously its own numbered item in this list, "HARDENED BASED ON CODE REVIEW, ROOT CAUSE NOT YET INDEPENDENTLY LIVE-CONFIRMED" — entry removed now that both parts are live-confirmed, remaining items renumbered accordingly).** `quotations.rbac.spec.ts`'s Q7 run twice on staging: both passed. Run 1 exercised only part (a) (the Company-dropdown-menu-close fix — confirmed via the log line `"Options menu opened after clear — closed via Escape"`). Run 2 organically exercised BOTH parts in sequence — Company cleared and retried, hit a second distinct 422 for Contact, cleared that too via the bounded 2-attempt loop, then saved successfully — the exact scenario part (b) was built for. Both fixes are now independently live-confirmed, not just code-reviewed.
- **`companies.spec.ts` CO4 flaky generic-error toast (2026-07-22).** Re-run twice on staging, isolated, no retries: 2/2 clean passes, no generic error toast either time. No longer treated as an open flake.
- **`deals.spec.ts` D4 ("verify payment math after marking installment received").** Re-run twice on staging, isolated, no retries: 2/2 clean passes.
- **`deals.spec.ts`'s test previously mislabeled "D141" ("change pipeline stage to Negotiation in edit") — label corrected: this test's real internal label is `D6`, confirmed via its own `logger.success('D6 passed')` line in both runs; `D141` never existed anywhere in the codebase.** Re-run twice on staging, isolated, no retries: 2/2 clean passes.
- **`quotations.spec.ts` Q21 ("admin should create and save a basic quotation with no contacts attached").** Re-run twice on staging, isolated, no retries: 2/2 clean passes, no generic error toast either time.

### Other known, permanent notes

- Annual Revenue field in `CompaniesPage` — **removed 2026-08-11 as a stale, non-issue** (previously listed here and in `APPLICATION_BUGS.md` as "commented out, confirmed prod bug"). Live-verified on QA: the field is not commented out, is actively filled in create+edit, and both the list view and detail-page right panel display it correctly (`Annual Revenue: INR 84.83975 L`, matching across views). See `APPLICATION_BUGS.md`'s "Investigated but not confirmed" section for the full write-up and the one real, narrower remaining gap (a skipped detail-page assertion due to Lakh-format string parsing — a test-framework item, not an app defect).
- QA sessions expire after ~1 hour — `rm -rf src/auth/storageStates/qa/` forces re-login.
- Clone lead/contact creates a pre-filled form — must change email/phone to avoid duplicate-validation errors.
- `saveQuickTask()` waits for the standalone task list view — use `saveQuickTaskFromEntityDetail()` when called from an entity detail panel.
