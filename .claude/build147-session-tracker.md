## Task Tracker — fix/history-sync-race-condition-20260824

| # | Task | Status | Notes |
|---|---|---|---|
| 1 | History-sync git-fetch race condition (syncHistory.ts) | Completed | Fixed + verified via local concurrent-race reproduction (3/3 clean). Live-CI confirmation still pending — see known-issues.md entry. |
| 2 | R36 residual flakiness under concurrent load (reports.spec.ts:902) | Completed | Root cause was NOT a report-engine delay — `verifyRunCountForEntity()`'s coarse ±1-day report window is shared with every Lead any other concurrent test creates, so a before/after total delta was never a safe comparison under `--workers=2`. Fixed by scoping the report to one uniquely-tagged entity via a new optional `filters` param (exact-match on a timestamped Last Name), applied to both R36 and its RBAC sibling R64. Also fixed a second bug this surfaced live: `getReportTotalFromHeader()` couldn't represent a true zero-count report (the header element doesn't render at all when a report matches 0 records — already documented elsewhere in this file); it now races against the app's own "no data" message. Added a small bounded retry (`waitForReportTotalBelow`) as defense-in-depth on top of the real fix. Verified live on staging: R36 3/3 clean, R64 1/1 clean, non-delete Lead run-count test re-confirmed non-regressed. Full writeup in known-issues.md's revised Sandbox Build #147 entry. |
| 3 | Issue 3 reopened — reports.spec.ts:291 recurrence | Completed (both symptoms honestly concluded — see note) | This test had TWO separate, unrelated recurring symptoms; both are now closed, but not the same way. **(a) `safeWaitForURL` timeout after clicking Save — ROOT-CAUSED AND FIXED.** Live reproduction (5 fresh attempts, `--workers=1 --retries=0`) found a genuine, 100% reproducible (5/5) Kylas backend defect: the first `POST /v3/reports` for a Meeting-type report always returns `HTTP 500 code=01403004`, and an identical immediate retry always succeeds (`201`). Confirmed backend-side (no orphan data, nothing differs between the two requests) and confirmed Meeting-specific (Lead/Deal/Contact/Call log: 20/20 clean, zero 500s). Documented as `APPLICATION_BUGS.md` entry #4. Mitigated test-side with a narrowly-scoped bounded retry, `ReportsPage.clickSaveButtonForCreateWithBackendRetry()` (fires only on this exact 500+code signature, max 2 attempts), applied in `createReport()` so every caller benefits. Verified live on staging: 5/5 runs passed, the 500 occurred and was absorbed by the retry in all 5. **(b) The timeout previously mislabeled "fixture-setup" — INVESTIGATED, NOT REPRODUCED (0/9), closed as an inconclusive rare flake, NOT fixed.** Correction: the stack trace actually points to fixture TEARDOWN (`context.close()`, after the test body already ran), not setup — an earlier note in this tracker mischaracterized it from a truncated log tail. Confirmed unrelated to (a) and unrelated to the already-documented "Concurrent-Worker Credential File Race" (different symptom shape — a hang, not a save-failure/re-login). Reproduction attempted 9 times total (5 isolated `--workers=1`, 4 under genuine single-invocation `--workers=2` concurrency including a Meeting-specific run) — 0/9 reproduced. Leading theory (not proven): host resource contention from this session's own earlier overlapping investigation processes, not a defect in shared fixture code. No fix applied to `src/fixtures/index.ts`/`authManager.ts` — insufficient evidence to justify changing code shared by every test in the suite. Full writeup in known-issues.md's revised Sandbox Build #147 entry. |
| 4 | 2 new flaky Companies RBAC tests (companies.rbac.spec.ts:126, :190) | Completed (independent findings — one fixed, one closed inconclusive) | Investigated separately per instruction not to assume a shared cause — they were unrelated. **COR7 (`:126`) — REAL BUG, FIXED.** Real failure (from CI log) was `assertCompanyExistsInList()` returning false right after an edit-save, NOT the edit-button visibility the test name suggests. Root cause: `CompaniesPage.saveEditedCompany()` had no network confirmation, only a modal-hidden check — the exact same bug class already found and fixed in `LeadsPage.saveEditedLead()` (2026-07-15), never applied to Companies. Fixed by mirroring that exact pattern (arm+await `PUT /v1/companies/{id}` before declaring success). Verified: full `companies.rbac.spec.ts` (22 tests) under real `--workers=2` — 22/22 passed, including COR7 and the analogous COR15. **Flagged, not fixed:** `ContactsPage.saveEditedContact()` has the identical gap and IS currently exercised by 2 real call sites (`contacts.rbac.spec.ts:131`, `:608`) — a live, unpatched risk, out of scope for this 2-test investigation. **COR9 (`:190`) — NOT a Companies bug, closed as an already-documented infrastructure class.** Real failure was `Test timeout of 120000ms exceeded while setting up "restrictedPage"` / `browser.newContext: Test ended` — a fixture-setup hang with zero Companies/Tasks code on the stack. Matches a broader trigger of the already-documented 2026-07-19 "58 fixture-timeout failures" class (partially fixed then, not fully closed). Not reproduced — the original needed full 379-test-suite load; a 22-test batch under real `--workers=2` isn't comparable and came back clean. No code change (same discipline as tracker row 3b — insufficient evidence to touch shared fixture code). Full writeup in known-issues.md's Sandbox Build #147 entry (item 4). |
| 5 | dev.yml / staging-promotion-gate.yml missing permissions fix | Completed | Applied the identical 2-4 line pattern used on the other 5 workflows: `permissions: contents: write` on the job + `with: token: ${{ secrets.PIPELINE_TOKEN }}` on the `actions/checkout@v5` step. `dev.yml`'s `run-tests` job and `staging-promotion-gate.yml`'s `test-staging` job specifically (its `promote-to-prod` job already had this). Nothing else changed in either file — YAML validity confirmed for both. All 7 workflows invoking `history:sync` now have write access. Live confirmation (a real push actually succeeding) requires an actual `dev`/staging-promotion CI run, which this session can't trigger. |

---

This file tracks in-progress work across this session on `fix/history-sync-race-condition-20260824`. See `.claude/known-issues.md` for full investigation detail and evidence on each item as it's completed.
